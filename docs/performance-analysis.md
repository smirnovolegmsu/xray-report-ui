# 🔍 Анализ производительности и оптимизация

## 📊 Обзор проблем

После глубокого анализа кода выявлены критические узкие места, которые замедляют работу приложения:

### Критические проблемы (высокий приоритет)

1. **Отсутствие кэширования на бэкенде** - каждый запрос читает все CSV файлы заново
2. **Избыточные запросы с фронтенда** - множественные компоненты делают запросы каждые 5-30 секунд
3. **Синхронная обработка CSV** - блокирует обработку других запросов
4. **Дублирование запросов** - несколько компонентов запрашивают одни и те же данные

---

## 🔴 Узкие места по времени выполнения

### 1. Бэкенд: Чтение CSV файлов без кэширования

**Проблема:**
- Функция `load_dashboard_data()` читает все CSV файлы при каждом запросе
- Для 7-31 дня это означает чтение 7-31 файлов `usage_*.csv`, `conns_*.csv`, `report_*.csv`, `domains_*.csv`
- Каждый файл может содержать тысячи строк
- Время выполнения: **500ms - 3000ms** на запрос

**Код:**
```python
# app.py:656-897
def load_dashboard_data(days: int = 7, user_filter: str = None):
    # Читает все CSV файлы синхронно при каждом запросе
    for i, date_key in enumerate(date_keys):
        fpath = os.path.join(usage_dir, f"usage_{date_key}.csv")
        for row in _read_csv_dict(fpath):  # Медленная операция
            # Обработка...
```

**Влияние:**
- Запрос `/api/dashboard` выполняется 500-3000ms
- Блокирует обработку других запросов
- При нескольких одновременных запросах сервер перегружается

---

### 2. Фронтенд: Избыточные запросы с короткими интервалами

**Проблема:**
- Компоненты делают запросы каждые 5-30 секунд
- Нет централизованного кэширования (SWR не используется)
- Дублирование запросов между компонентами

**Статистика запросов:**
```
LiveNow:          каждые 5 секунд  (getLiveNow, getLiveSeries)
LiveCharts:       каждые 5 секунд  (getLiveSeries)
EventsTable:      каждые 10 секунд (getEvents)
EventsStats:      каждые 30 секунд (getEventsStats)
StatusBadges:     каждые 10 секунд (getSystemStatus)
SystemResources:  каждые 30 секунд (getSystemResources)
UserStatsCards:   каждые 30 секунд (getUserStats)
ApiStatusChecker: каждые 30 секунд (ping + getSystemResources)
```

**Влияние:**
- При открытой странице: **~20-30 запросов в минуту**
- Каждый запрос вызывает обработку CSV на бэкенде
- Перегрузка сервера при нескольких открытых вкладках

---

### 3. Бэкенд: Синхронная обработка больших CSV файлов

**Проблема:**
- Чтение CSV происходит синхронно
- Блокирует event loop Flask
- Нет параллельной обработки

**Код:**
```python
# app.py:481-494
def _read_csv(path: str):
    with open(path, "r", encoding="utf-8") as f:
        lines = [ln.rstrip("\n") for ln in f.readlines() if ln.strip()]  # Читает весь файл в память
    # Обработка...
```

**Влияние:**
- Большие CSV файлы (>10MB) читаются полностью в память
- Блокирует обработку других запросов
- Высокое использование памяти

---

### 4. Фронтенд: Отсутствие кэширования запросов

**Проблема:**
- SWR установлен в зависимостях, но не используется
- Каждый компонент делает независимые запросы
- Нет дедупликации запросов

**Пример:**
```typescript
// Несколько компонентов запрашивают одни и те же данные
MetricsCards: apiClient.getDashboard({ days: 14 })
TrafficChart: apiClient.getUsageDashboard(...)
UserStatsCards: apiClient.getUserStats()
```

**Влияние:**
- Дублирование запросов к одному эндпоинту
- Лишняя нагрузка на бэкенд
- Медленная загрузка страницы

---

## 💾 Узкие места по памяти

### 1. Загрузка всех CSV файлов в память

**Проблема:**
- `_read_csv_dict()` читает весь файл в память
- Для 31 дня это может быть 31 файл × несколько MB = **100+ MB**

**Код:**
```python
def _read_csv_dict(path: str) -> List[Dict[str, str]]:
    header, rows, err = _read_csv(path)  # Читает весь файл
    result = []
    for row in rows:  # Создает словари для каждой строки
        d = {}
        for i, h in enumerate(header):
            d[h] = row[i]
        result.append(d)
    return result  # Возвращает весь список в памяти
```

**Влияние:**
- Высокое использование памяти при обработке
- Риск OOM (Out of Memory) на серверах с ограниченной памятью

---

### 2. Накопление данных в live_buffer

**Проблема:**
- `live_buffer` накапливает данные без ограничений
- Может расти бесконечно

**Код:**
```python
# app.py:3151-3203
def _update_live_buffer():
    with live_buffer_lock:
        live_buffer["online_users"].append({...})
        # Keep only last LIVE_BUFFER_SIZE points
        if len(live_buffer[metric]) > LIVE_BUFFER_SIZE:
            live_buffer[metric] = live_buffer[metric][-LIVE_BUFFER_SIZE:]
```

**Влияние:**
- Постоянное использование памяти для буфера
- Риск утечки памяти при ошибках

---

## 🎯 Рекомендации по оптимизации

### Приоритет 1: Критичные (немедленно)

#### 1.1. Добавить кэширование на бэкенде

**Решение:**
- Использовать `functools.lru_cache` для кэширования результатов
- Инвалидация кэша при изменении данных
- TTL кэша: 30-60 секунд для live данных, 5 минут для исторических

**Реализация:**
```python
from functools import lru_cache
from datetime import datetime, timedelta

# Кэш для dashboard данных
_dashboard_cache = {}
_cache_timestamps = {}

def load_dashboard_data_cached(days: int = 7, user_filter: str = None):
    cache_key = f"dashboard_{days}_{user_filter}"
    now = datetime.now()
    
    # Проверяем кэш
    if cache_key in _dashboard_cache:
        cache_time = _cache_timestamps.get(cache_key)
        if cache_time and (now - cache_time).total_seconds() < 60:
            return _dashboard_cache[cache_key]
    
    # Загружаем данные
    data = load_dashboard_data(days, user_filter)
    
    # Сохраняем в кэш
    _dashboard_cache[cache_key] = data
    _cache_timestamps[cache_key] = now
    
    return data
```

**Ожидаемый эффект:**
- Снижение времени ответа с 500-3000ms до **10-50ms** (из кэша)
- Снижение нагрузки на диск на **90-95%**
- Увеличение пропускной способности в **10-20 раз**

---

#### 1.2. Использовать SWR на фронтенде

**Решение:**
- Настроить SWR для всех API запросов
- Централизованное кэширование и дедупликация
- Автоматическая ревалидация

**Реализация:**
```typescript
// lib/api-swr.ts
import useSWR from 'swr';
import { apiClient } from './api';

const fetcher = (url: string) => apiClient.request(url).then(res => res.data);

export function useDashboard(days?: number) {
  const { data, error, isLoading } = useSWR(
    `/dashboard?days=${days || 14}`,
    fetcher,
    {
      refreshInterval: 30000, // 30 секунд
      revalidateOnFocus: false,
      dedupingInterval: 5000, // Дедупликация запросов
    }
  );
  
  return { data, error, isLoading };
}
```

**Ожидаемый эффект:**
- Снижение количества запросов на **70-80%**
- Дедупликация одновременных запросов
- Улучшение UX (мгновенная загрузка из кэша)

---

#### 1.3. Оптимизировать чтение CSV

**Решение:**
- Использовать потоковое чтение вместо загрузки всего файла
- Парсинг по мере чтения
- Ограничение размера буфера

**Реализация:**
```python
import csv
from typing import Iterator, Dict

def _read_csv_dict_streaming(path: str) -> Iterator[Dict[str, str]]:
    """Читает CSV потоково, не загружая весь файл в память"""
    try:
        with open(path, "r", encoding="utf-8") as f:
            reader = csv.DictReader(f)
            for row in reader:
                yield {k.strip().lower(): v.strip() for k, v in row.items()}
    except Exception:
        return

# Использование:
for row in _read_csv_dict_streaming(fpath):
    # Обработка строки
    process_row(row)
```

**Ожидаемый эффект:**
- Снижение использования памяти на **80-90%**
- Возможность обработки больших файлов без OOM
- Улучшение отзывчивости сервера

---

### Приоритет 2: Важные (в ближайшее время)

#### 2.1. Увеличить интервалы обновления

**Решение:**
- Увеличить интервалы для не-критичных данных
- Использовать адаптивные интервалы (больше при неактивности)

**Рекомендуемые интервалы:**
```
LiveNow:         10 секунд (было 5)
LiveCharts:      10 секунд (было 5)
EventsTable:     30 секунд (было 10)
EventsStats:     60 секунд (было 30)
StatusBadges:    30 секунд (было 10)
SystemResources: 60 секунд (было 30)
UserStatsCards:  60 секунд (было 30)
```

**Ожидаемый эффект:**
- Снижение количества запросов на **50%**
- Меньше нагрузка на сервер

---

#### 2.2. Добавить батчинг запросов

**Решение:**
- Объединять несколько запросов в один
- Создать эндпоинт `/api/batch` для множественных запросов

**Реализация:**
```typescript
// lib/api-batch.ts
export async function batchRequests(requests: Array<{key: string, url: string}>) {
  const response = await apiClient.post('/batch', { requests });
  return response.data.results;
}
```

**Ожидаемый эффект:**
- Снижение количества HTTP запросов на **60-70%**
- Уменьшение latency за счет одного запроса вместо нескольких

---

#### 2.3. Оптимизировать передачу данных

**Решение:**
- Использовать сжатие (gzip) для больших ответов
- Отправлять только необходимые поля
- Использовать пагинацию для больших списков

**Реализация:**
```python
from flask import gzip
import json

@app.get("/api/dashboard")
@compress_response()  # Добавить gzip сжатие
def api_dashboard():
    # Отправлять только необходимые поля
    data = load_dashboard_data_cached()
    return ok({
        "meta": data["meta"],
        "global": {
            "daily_traffic_bytes": data["global"]["daily_traffic_bytes"],
            # Только нужные поля
        }
    })
```

**Ожидаемый эффект:**
- Снижение размера ответов на **60-80%**
- Быстрее передача данных по сети

---

### Приоритет 3: Желательные (по возможности)

#### 3.1. Использовать базу данных вместо CSV

**Решение:**
- Мигрировать данные в SQLite или PostgreSQL
- Индексы для быстрого поиска
- Оптимизированные запросы

**Ожидаемый эффект:**
- Снижение времени запросов с 500-3000ms до **10-50ms**
- Масштабируемость для больших объемов данных

---

#### 3.2. Добавить Redis для кэширования

**Решение:**
- Использовать Redis для распределенного кэширования
- TTL для автоматической инвалидации
- Кэширование часто запрашиваемых данных

**Ожидаемый эффект:**
- Еще больше снижение времени ответа
- Возможность масштабирования на несколько серверов

---

#### 3.3. Оптимизировать фронтенд бандл

**Решение:**
- Code splitting для больших библиотек
- Lazy loading компонентов
- Оптимизация изображений и ресурсов

**Ожидаемый эффект:**
- Быстрее загрузка страницы
- Меньше использование памяти в браузере

---

## 📈 Ожидаемые результаты после оптимизации

### Производительность

| Метрика | До | После | Улучшение |
|---------|-----|-------|-----------|
| Время ответа `/api/dashboard` | 500-3000ms | 10-50ms | **98%** |
| Количество запросов/мин | 20-30 | 5-10 | **70%** |
| Использование памяти | 100-200MB | 20-40MB | **80%** |
| Нагрузка на диск | Высокая | Низкая | **90%** |
| Пропускная способность | 10 req/s | 100+ req/s | **10x** |

### Пользовательский опыт

- **Мгновенная загрузка** данных из кэша
- **Плавные обновления** без задержек
- **Меньше нагрузка** на сервер
- **Стабильная работа** при нескольких пользователях

---

## 🚀 План внедрения

### Фаза 1: Критичные оптимизации (1-2 дня)

1. ✅ Добавить кэширование на бэкенде
2. ✅ Настроить SWR на фронтенде
3. ✅ Оптимизировать чтение CSV

### Фаза 2: Важные оптимизации (2-3 дня)

4. ✅ Увеличить интервалы обновления
5. ✅ Добавить батчинг запросов
6. ✅ Оптимизировать передачу данных

### Фаза 3: Желательные оптимизации (по необходимости)

7. ⏳ Миграция на БД (если нужно)
8. ⏳ Redis кэширование (если нужно)
9. ⏳ Оптимизация бандла (если нужно)

---

## 📝 Мониторинг производительности

### Метрики для отслеживания

1. **Время ответа API** - должно быть < 100ms для кэшированных запросов
2. **Количество запросов** - должно снизиться на 70%+
3. **Использование памяти** - должно снизиться на 80%+
4. **CPU использование** - должно снизиться на 50%+

### Инструменты

- Flask logging для времени ответа
- Browser DevTools для анализа запросов
- System monitoring (htop, iotop) для ресурсов

---

## 🔧 Технические детали реализации

См. отдельные документы:
- [Кэширование на бэкенде](./backend-caching.md)
- [SWR на фронтенде](./frontend-swr.md)
- [Оптимизация CSV](./csv-optimization.md)
