# Анализ файлов в корне проекта

**Дата:** 2026-01-25  
**Ветка:** `claude/review-code-errors-WLQUL`

## 📋 Текущее состояние корня:

### ✅ Обязательные файлы (правильно в корне):
- ✅ `app.py` - главный Flask файл (2894 строки)
- ✅ `README.md` - основная документация проекта
- ✅ `.gitignore` - правила Git

### ❌ Файлы, которые НЕ должны быть в корне:

#### Документация (6 файлов):
1. ❌ `CIRCULAR_PROGRESS_INTEGRATION.md` - документация по интеграции
2. ❌ `CSS_FIXES_SUMMARY.md` - сводка исправлений CSS
3. ❌ `GITIGNORE_CHECK_REPORT.md` - отчет о проверке .gitignore
4. ❌ `GITIGNORE_IMPROVEMENTS.md` - улучшения .gitignore
5. ❌ `PROJECT_STRUCTURE_FOR_CLAUDE.md` - описание структуры для Claude
6. ❌ `STRUCTURE_AUDIT.md` - аудит структуры проекта

#### Прототипы (1 файл):
7. ❌ `metric_variants_prototype.html` - HTML прототип метрик

---

## 🎯 Рекомендации:

### Что должно быть в корне проекта:

**Обязательно:**
- ✅ `app.py` - главный файл приложения
- ✅ `README.md` - основная документация
- ✅ `.gitignore` - правила Git

**Опционально (если используются):**
- `requirements.txt` - Python зависимости
- `pytest.ini` - конфигурация pytest
- `.env.example` - пример env файла
- `docker-compose.yml` - если используется Docker
- `LICENSE` - лицензия проекта

**НЕ должно быть в корне:**
- ❌ Множество документации (должна быть в `docs/`)
- ❌ Прототипы и примеры (должны быть в `docs/` или `_archive_legacy/`)
- ❌ Временные файлы
- ❌ Отчеты и аудиты (должны быть в `docs/`)

---

## 🔧 Что нужно сделать:

### Вариант 1: Переместить всё в `docs/` (рекомендуется)

```bash
# Создать подпапку для корневых документов
mkdir -p docs/root/

# Переместить документацию
mv CIRCULAR_PROGRESS_INTEGRATION.md docs/root/
mv CSS_FIXES_SUMMARY.md docs/root/
mv GITIGNORE_CHECK_REPORT.md docs/root/
mv GITIGNORE_IMPROVEMENTS.md docs/root/
mv PROJECT_STRUCTURE_FOR_CLAUDE.md docs/root/
mv STRUCTURE_AUDIT.md docs/root/

# Переместить прототип
mv metric_variants_prototype.html docs/root/  # или docs/archive/
```

### Вариант 2: Переместить в `docs/archive/` (если это архивные документы)

```bash
# Переместить в архив
mv *.md docs/archive/  # кроме README.md
mv *.html docs/archive/
```

### Вариант 3: Оставить важные, остальные в docs/

```bash
# Оставить только самые важные в корне
# Остальные переместить в docs/
```

---

## 📊 Стандартная структура корня проекта:

```
project-root/
├── app.py                    # ✅ Главный файл
├── README.md                 # ✅ Основная документация
├── .gitignore               # ✅ Git правила
├── requirements.txt         # Опционально
├── pytest.ini              # Опционально
├── .env.example             # Опционально
│
├── backend/                 # Backend код
├── frontend/                # Frontend код
├── docs/                    # Вся документация
├── tests/                   # Тесты
└── scripts/                 # Скрипты
```

---

## ✅ Итог:

**Текущее состояние:** ❌ **7 лишних файлов в корне**

**Рекомендация:** Переместить всю документацию и прототипы в `docs/` для лучшей организации проекта.

**После очистки в корне останется:**
- ✅ `app.py`
- ✅ `README.md`
- ✅ `.gitignore`
- ✅ Папки: `backend/`, `frontend/`, `docs/`, `static/`, `templates/`, `_archive_legacy/`

---

## 🎯 План действий:

1. **Создать структуру:** `docs/root/` для корневых документов
2. **Переместить:** Все `.md` файлы (кроме README.md) в `docs/root/`
3. **Переместить:** `metric_variants_prototype.html` в `docs/root/` или `docs/archive/`
4. **Обновить:** Ссылки в README.md если нужно
5. **Закоммитить:** Изменения

**Результат:** Чистый корень проекта с только необходимыми файлами.
