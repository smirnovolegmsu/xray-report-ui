# Редизайн страницы "События" — Xray Admin Panel

## A) Диагностика текущего экрана

### A.1 Структурные проблемы интерфейса

**Проблема 1: Вертикальная перегрузка**
- **Текущее состояние**: Статистика (4 большие карточки) → График → Фильтры → Таблица — все в вертикальном списке
- **Влияние**: Пользователю нужно прокрутить ~800-1000px до таблицы событий
- **Доказательство**: `page.tsx:68-203` — все компоненты в `space-y-6` вертикальном layout
- **Исправление**: Переделать на 2-колоночный layout с sidebar для статистики

**Проблема 2: Статистика занимает слишком много места**
- **Текущее состояние**: 4 карточки в ряд (`grid-cols-4`) + блок "По типам" + блок "Критичные" = ~600px высоты
- **Влияние**: Критичная информация (активные инциденты) скрыта ниже первого экрана
- **Доказательство**: `events-stats.tsx:100-182` — три блока в вертикальном списке
- **Исправление**: Компактные карточки в sidebar (высота ~300px)

**Проблема 3: Слабая иерархия информации**
- **Текущее состояние**: Все компоненты равнозначны, нет выделения критичного
- **Влияние**: Пользователь не понимает, на что обратить внимание в первую очередь
- **Доказательство**: Нет визуального выделения активных инцидентов, все события в одной таблице
- **Исправление**: Блок "Критичные события" выше таблицы, подсветка активных инцидентов

**Проблема 4: Фильтры неочевидны**
- **Текущее состояние**: Фильтры в dropdown меню, активные фильтры видны только в кнопке
- **Влияние**: Пользователь не видит сразу, какие фильтры применены
- **Доказательство**: `page.tsx:128-186` — фильтры в `DropdownMenu`, активные показываются только как badge внутри кнопки
- **Исправление**: Фильтры как чипсы в header, всегда видимые

**Проблема 5: Отсутствие "active incident" статуса**
- **Текущее состояние**: Критичные события показываются списком без индикации актуальности
- **Влияние**: Непонятно, какие проблемы актуальны прямо сейчас
- **Доказательство**: `events-stats.tsx:149` — просто список событий, нет проверки времени
- **Исправление**: Добавить проверку `ts < 5 минут назад` и подсветку активных инцидентов

### A.2 Ошибки логики отображения

**Ошибка 1: `timeRange` не применяется к таблице**
- **Код**: `page.tsx:198-202` — `EventsTable` не получает `timeRange`
- **Влияние**: Таблица всегда показывает последние 100 событий, игнорируя выбранный период
- **Как проверить**: Выбрать период "1ч", проверить, что таблица показывает события старше 1 часа
- **Как исправить**: Передать `timeRange` в `EventsTable`, фильтровать события: `events.filter(e => new Date(e.ts) > cutoff)`

**Ошибка 2: Фильтры не применяются к графику**
- **Код**: `page.tsx:102` — `EventsTimeline` не получает `typeFilter`/`severityFilter`
- **Влияние**: График показывает все события, даже если применены фильтры
- **Как проверить**: Применить фильтр "ERROR", проверить, что график все еще показывает INFO события
- **Как исправить**: Передать фильтры в `EventsTimeline`, фильтровать `timeline` данные на клиенте

**Ошибка 3: Таблица использует нестабильный ключ**
- **Код**: `events-table.tsx:300` — `key={idx}` вместо уникального ключа
- **Влияние**: React может некорректно обновлять строки при изменении данных
- **Как проверить**: Отсортировать таблицу, проверить, что строки обновляются корректно
- **Как исправить**: Использовать `key={`${event.ts}-${event.type}-${event.action}`}`

**Ошибка 4: Нет группировки инцидентов**
- **Код**: `events-stats.tsx:149` — `recentCritical` показывается простым списком
- **Влияние**: Несколько событий одного инцидента показываются отдельно, сложно понять причину
- **Как проверить**: Если есть несколько `SERVICE_HEALTH: service_down` для одного сервиса, они показываются отдельно
- **Как исправить**: Реализовать группировку по правилам из раздела 5.1

**Ошибка 5: Нет дедупликации**
- **Код**: `events-table.tsx:299-324` — все события показываются как отдельные строки
- **Влияние**: Дубли событий засоряют таблицу
- **Как проверить**: Проверить логи на наличие дублей (одинаковые `type`+`action`+`service` в пределах 5 минут)
- **Как исправить**: Группировать дубли, показывать "[N похожих]" с возможностью развернуть

**Ошибка 6: Несогласованное форматирование портов**
- **Код**: `events-table.tsx:168, 175` — порты форматируются по-разному: `(порт ${event.port})` vs без префикса
- **Влияние**: Непоследовательность в отображении
- **Как проверить**: Проверить события `SERVICE_HEALTH` с портами, сравнить формат
- **Как исправить**: Унифицировать формат: всегда `сервис (порт N)`

**Ошибка 7: График не интерактивен**
- **Код**: `events-timeline.tsx:76-90` — нет обработчика клика на столбцы
- **Влияние**: Невозможно фильтровать таблицу по выбранному часу
- **Как проверить**: Кликнуть на столбец графика, проверить, что таблица не фильтруется
- **Как исправить**: Добавить `onClick` обработчик, фильтровать таблицу по выбранному часу

### A.3 Предупреждения и потенциальные проблемы

**Предупреждение 1: Нет виртуализации таблицы**
- **Текущее состояние**: Все события рендерятся сразу, даже если их 100+
- **Влияние**: При большом количестве событий таблица может тормозить
- **Как проверить**: Загрузить страницу с 100+ событиями, проверить производительность скролла
- **Как исправить**: Добавить виртуализацию через `react-window` или пагинацию

**Предупреждение 2: Неоднозначные временные метки**
- **Текущее состояние**: События показываются в формате `ru-RU`, но не указан часовой пояс
- **Влияние**: Пользователь не знает, в каком часовом поясе показано время
- **Как проверить**: Проверить формат `ts` в API (всегда ли UTC?)
- **Как исправить**: Добавить отображение часового пояса в header или показывать время в локальном часовом поясе браузера

**Предупреждение 3: Повторяющиеся "остановка/запуск"**
- **Текущее состояние**: Много событий `SYSTEM restart` подряд показываются отдельно
- **Влияние**: Сложно понять, что реально произошло (один перезапуск или несколько)
- **Как проверить**: Проверить логи на паттерн "restart → restart → restart"
- **Как исправить**: Группировать последовательные restart события в один инцидент

**Предупреждение 4: Конфликт версий**
- **Текущее состояние**: В комментариях `app.py:4` упоминается v2.0
- **Влияние**: Несоответствие документации и реальной версии
- **Как проверить**: Проверить `app.py:4` и реальную версию из API `/api/version`
- **Как исправить**: Обновить комментарии или добавить динамическое отображение версии

---

## 1. Ключевая цель экрана и KPI читабельности

### Цели
- **Время до обнаружения инцидента**: < 3 секунд (без прокрутки)
- **Количество скролла до первой диагностики**: 0 (все критичное на первом экране)
- **Плотность информации**: максимум смысла на минимальной площади
- **Скорость фильтрации**: 1 клик для переключения периода/типа/уровня

### Метрики успеха
- Пользователь видит активные инциденты без прокрутки
- Статистика всегда видна (sticky sidebar)
- График 24ч интерактивен и фильтрует таблицу
- Критичные события сгруппированы по инцидентам

---

## 2. Новый layout (описание зон) + ASCII-вайрфрейм

### Структура страницы

```
┌─────────────────────────────────────────────────────────────────────────┐
│ STICKY HEADER (фиксированный)                                          │
│ ┌─────────────────────────────────────────────────────────────────────┐ │
│ │ [☰] События                    [1ч] [6ч] [24ч] [7д]  [🔍 Поиск...] │ │
│ │                                  [v2.1] [prod]                      │ │
│ └─────────────────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│ ┌──────────────────────────────────────┐ ┌──────────────────────────┐ │
│ │ MAIN CONTENT AREA                    │ │ RIGHT SIDEBAR (sticky)  │ │
│ │                                      │ │ ┌──────────────────────┐ │ │
│ │ ┌──────────────────────────────────┐ │ │ │ 📊 Статистика       │ │ │
│ │ │ ВИЗУАЛИЗАЦИЯ 24ч (интерактивная) │ │ │ │                     │ │ │
│ │ │ [Stacked histogram / Heatmap]   │ │ │ │ Всего: 1,234        │ │ │
│ │ │ [Клик → фильтр таблицы]          │ │ │ │ ⚠️ Errors: 12       │ │ │
│ │ └──────────────────────────────────┘ │ │ │ ⚡ Warn: 45          │ │ │
│ │                                      │ │ │ ℹ️ Info: 1,177       │ │ │
│ │ ┌──────────────────────────────────┐ │ │ │                     │ │ │
│ │ │ 🔴 КРИТИЧНЫЕ СОБЫТИЯ             │ │ │ │ По типам:           │ │ │
│ │ │ ┌──────────────────────────────┐ │ │ │ │ USER: 234           │ │ │
│ │ │ │ ▼ Инцидент #1: Сервис down   │ │ │ │ │ SYSTEM: 45          │ │ │
│ │ │ │   Xray недоступен (2ч 15м)   │ │ │ │ │ ERROR: 12           │ │ │
│ │ │ │   [3 события] [Перейти]      │ │ │ │ │ ...                 │ │ │
│ │ │ └──────────────────────────────┘ │ │ │ │                     │ │ │
│ │ │ ┌──────────────────────────────┐ │ │ │ │ Health Summary:      │ │ │
│ │ │ │ ▶ Инцидент #2: Порт 443      │ │ │ │ │ ✅ UI: UP            │ │ │
│ │ │ │   Медленный ответ (15м)      │ │ │ │ │ ✅ Xray: UP          │ │ │
│ │ │ │   [1 событие] [Перейти]      │ │ │ │ │ ⚠️ NextJS: SLOW     │ │ │
│ │ │ └──────────────────────────────┘ │ │ │ └──────────────────────┘ │ │
│ │ └──────────────────────────────────┘ │ │                          │ │
│ │                                      │ │                          │ │
│ │ ┌──────────────────────────────────┐ │ │                          │ │
│ │ │ ТАБЛИЦА СОБЫТИЙ (компактная)     │ │ │                          │ │
│ │ │ [Виртуализация / Пагинация]      │ │ │                          │ │
│ │ │ Время | Тип | Уровень | Описание│ │ │                          │ │
│ │ │ ...                               │ │ │                          │ │
│ │ └──────────────────────────────────┘ │ │                          │ │
│ │                                      │ │                          │ │
│ └──────────────────────────────────────┘ └──────────────────────────┘ │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Зоны страницы

1. **Sticky Header** (высота: 64px)
   - Название страницы + иконка меню
   - Переключатели периода (1ч/6ч/24ч/7д) — чипсы
   - Поиск (компактный, с иконкой)
   - Фильтры-чипсы (активные фильтры видны как badges)
   - Версия/среда (опционально, справа)

2. **Right Sidebar** (ширина: 280px, sticky)
   - Компактные карточки метрик (Всего/Errors/Warn/Info)
   - Распределение по типам (компактный список)
   - Health Summary по сервисам (UI/Xray/NextJS)

3. **Main Content** (flex-1)
   - **Визуализация 24ч** (высота: 200px)
   - **Критичные события** (группировка по инцидентам)
   - **Таблица событий** (виртуализация, компактные строки)

---

## 3. Компоненты

### 3.1 Sticky Header
**Назначение**: Быстрый доступ к фильтрам и переключению периода  
**Данные**: Локальное состояние (period, filters)  
**Состояния**:
- `loading`: Disabled состояние для переключателей
- `active`: Подсветка активного периода/фильтра
- `empty`: Скрытие неактивных фильтров

**Элементы**:
- Переключатели периода: `[1ч] [6ч] [24ч] [7д]` (Button variants: default/outline)
- Поиск: Input с иконкой Search, очистка через X
- Фильтры-чипсы: Badge компоненты (Type, Severity, Service) — показываются только при активных фильтрах
- Кнопка "Сбросить фильтры" (появляется при активных фильтрах)

### 3.2 Right Sidebar — Статистика
**Назначение**: Компактный обзор метрик без прокрутки  
**Данные**: `EventsStats` из API (`/api/events/stats?hours={hours}`)  
**Состояния**:
- `loading`: Skeleton loaders для карточек
- `error`: Показ ошибки в одной карточке, остальные скрыты
- `empty`: Показ нулей

**Компоненты**:
- **Метрики** (4 карточки, компактные):
  - Всего: `stats.total`
  - Errors: `stats.errors` (красный)
  - Warnings: `stats.warnings` (желтый)
  - Info: `stats.info` (синий)
- **По типам**: Компактный список с Badge (тип: количество), сортировка по убыванию
- **Health Summary**: Статусы сервисов (UP/DOWN/SLOW) на основе последних `SERVICE_HEALTH` событий

### 3.3 Визуализация 24ч
**Назначение**: График распределения событий по времени с интерактивностью  
**Данные**: `EventsStatsResponse.timeline` (массив точек по часам)  
**Состояния**:
- `loading`: Skeleton с анимацией
- `empty`: Сообщение "Нет данных за период"
- `selected`: Подсветка выбранного интервала

**Варианты визуализации** (см. раздел 4)

### 3.4 Критичные события (Incidents)
**Назначение**: Группировка критичных событий в инциденты  
**Данные**: Агрегация из `stats.recentCritical` + логика группировки  
**Состояния**:
- `loading`: Skeleton списка
- `empty`: Сообщение "Нет критичных событий"
- `expanded`: Развернутый инцидент с деталями

**Логика группировки**:
- Группировка по `correlation_id` (если есть)
- Или эвристика: `type + service + port + окно 30 минут`
- Показ: причина, длительность, количество событий, последний статус

**Компонент**: Collapsible карточки с:
- Заголовок: "Инцидент #N: [тип] [сервис]"
- Подзаголовок: Время начала, длительность
- Счетчик событий: "[N событий]"
- CTA: "Перейти к событиям инцидента" (фильтрует таблицу)

### 3.5 Таблица событий
**Назначение**: Детальный список событий с фильтрацией  
**Данные**: `Event[]` из `/api/events?limit=100` + фильтрация на клиенте  
**Состояния**:
- `loading`: Skeleton таблицы
- `empty`: Empty state с предложением изменить фильтры
- `filtered`: Показ количества отфильтрованных событий

**Колонки** (компактные):
- Время: `HH:mm:ss` (моноширинный, 80px)
- Тип: Иконка + сокращение типа (80px)
- Уровень: Badge (ERROR/WARN/INFO) (60px)
- Описание: `interpretEvent(event)` (flex-1, truncate)

**Особенности**:
- Виртуализация через `react-window` или пагинация (50 событий на страницу)
- Дефолтная сортировка: `ts DESC` (новые сверху)
- Группировка дублей: показ "[N похожих]" с возможностью развернуть
- Подсветка строк: ERROR — красный фон, WARN — желтый фон

---

## 4. Визуализация 24ч — варианты и почему

### Вариант 1: Stacked Histogram (рекомендуемый)

**Описание**: Столбчатая диаграмма с наложенными слоями по уровням  
**Визуализация**:
```
     │
 100 │     ████
     │     ████
  50 │  ████▓▓▓▓
     │  ████▓▓▓▓
   0 └─────────────────────
     00  06  12  18  24
```

**Легенда**:
- Синий: INFO события
- Желтый: WARN события (поверх INFO)
- Красный: ERROR события (поверх WARN)

**Интерактивность**:
- Hover: Tooltip с точными значениями (Total/Errors/Warnings/Info)
- Click: Фильтрация таблицы по выбранному часу
- Выделение: Подсветка выбранного столбца

**Плюсы**:
- Показывает распределение по уровням
- Интуитивно понятно
- Легко реализовать (Chart.js / Recharts)

**Минусы**:
- При большом количестве INFO может быть нечитаемо

---

### Вариант 2: Timeline Heatmap

**Описание**: Тепловая карта с интенсивностью по цвету  
**Визуализация**:
```
     │
 24ч │ ░░░░▓▓▓▓████
     │ ░░░░▓▓▓▓████
     │ ░░░░▓▓▓▓████
   0 └─────────────────────
     00  06  12  18  24
```

**Цветовая схема**:
- Серый: 0 событий
- Зеленый: 1-10 событий (INFO)
- Желтый: 11-50 событий (WARN)
- Красный: 51+ событий (ERROR)

**Интерактивность**:
- Hover: Tooltip с деталями
- Click: Фильтрация таблицы
- Выделение: Обводка выбранного блока

**Плюсы**:
- Компактно
- Хорошо показывает пики активности
- Работает при большом объеме данных

**Минусы**:
- Менее информативно по уровням (нужна дополнительная легенда)

---

### Рекомендация: **Вариант 1 (Stacked Histogram)**

**Почему**:
- Показывает распределение по уровням критичности
- Соответствует задаче "быстро понять, насколько плохо"
- Легко добавить интерактивность

**Реализация**:
- Использовать `recharts` или `chart.js`
- Данные: `timeline` из API (уже агрегированы по часам)
- Добавить обработчик клика для фильтрации таблицы

---

## 5. Правила агрегации/дедупликации и подсветки критичности

### 5.1 Группировка инцидентов

**Правило 1: По correlation_id**
- Если событие имеет поле `correlation_id`, группировать все события с одинаковым `correlation_id`
- Показывать как один инцидент с количеством событий

**Правило 2: Эвристическая группировка (если нет correlation_id)**
- Группировать события, если выполняются условия:
  - `type` одинаковый
  - `service` одинаковый (если есть)
  - `port` одинаковый (если есть)
  - Разница во времени < 30 минут
  - `severity` в группе: ERROR или WARN

**Пример группировки**:
```javascript
// События:
// 1. SERVICE_HEALTH: service_down (Xray, 10:00)
// 2. SERVICE_HEALTH: service_down (Xray, 10:05)
// 3. SERVICE_HEALTH: service_up (Xray, 10:15)
// → Группа: "Инцидент: Xray недоступен (10:00-10:15, 3 события)"
```

### 5.2 Дедупликация в таблице

**Правило**: Показывать дубли как свернутые строки

**Критерий дубля**:
- `type` + `action` + `service` + `port` одинаковые
- Разница во времени < 5 минут
- `severity` одинаковый

**Отображение**:
```
[10:05] SYSTEM  ERROR  Перезапуск сервиса: Xray
[10:07] SYSTEM  ERROR  Перезапуск сервиса: Xray  [2 похожих ▼]
```

При клике на "[2 похожих ▼]": развернуть все дубли.

### 5.3 Подсветка критичности

**Уровни критичности**:

1. **Критично (ERROR)**:
   - Фон строки: `bg-red-50 dark:bg-red-950/20`
   - Badge: `destructive` variant
   - Иконка: AlertCircle (красный)

2. **Предупреждение (WARN)**:
   - Фон строки: `bg-yellow-50 dark:bg-yellow-950/20`
   - Badge: `secondary` variant (желтый)
   - Иконка: AlertTriangle (желтый)

3. **Информация (INFO)**:
   - Фон строки: прозрачный (по умолчанию)
   - Badge: `outline` variant
   - Иконка: Info (синий)

**Активные инциденты**:
- Инцидент считается активным, если:
  - Последнее событие < 5 минут назад
  - Или последний статус = DOWN (для SERVICE_HEALTH)
- Подсветка: Красная обводка карточки инцидента + иконка "активно"

---

## 6. Список исправлений текущих ошибок/предупреждений

### 6.1 Критические баги (найдены в коде)

| Проблема | Файл/Строка | Влияние | Как проверить | Как исправить |
|----------|-------------|---------|---------------|---------------|
| **`timeRange` не передается в `EventsTable`** | `page.tsx:198-202` | Таблица всегда показывает последние 100 событий, игнорируя выбранный период (1ч/6ч/24ч/7д) | Выбрать период "1ч", проверить, что таблица показывает события только за последний час | Передать `timeRange` в `EventsTable` и фильтровать события по времени на клиенте: `events.filter(e => new Date(e.ts) > cutoff)` |
| **График не получает фильтры типа/уровня** | `page.tsx:102`, `events-timeline.tsx:16` | График показывает все события, даже если применены фильтры `typeFilter`/`severityFilter` | Применить фильтр "ERROR", проверить, что график все еще показывает INFO события | Передать `typeFilter` и `severityFilter` в `EventsTimeline`, фильтровать `timeline` данные на клиенте |
| **Таблица использует `key={idx}` вместо уникального ключа** | `events-table.tsx:300` | React может некорректно обновлять строки при изменении данных, возможны баги с состоянием | Отсортировать таблицу, проверить, что строки обновляются корректно | Использовать уникальный ключ: `key={`${event.ts}-${event.type}-${event.action}`}` или `event.ts + event.type + idx` |
| **Нет фильтрации по времени в таблице** | `events-table.tsx:46-57` | `loadEvents()` всегда загружает последние 100 событий без учета `timeRange` | Выбрать период "1ч", проверить, что таблица показывает события старше 1 часа | Добавить фильтрацию: `events.filter(e => { const eventTime = new Date(e.ts); return eventTime > cutoff; })` где `cutoff = now - timeRange hours` |
| **График не интерактивен (нет клика)** | `events-timeline.tsx:76-90` | Невозможно фильтровать таблицу по выбранному часу на графике | Кликнуть на столбец графика, проверить, что таблица не фильтруется | Добавить `onClick` обработчик: `onClick={() => setSelectedHour(point.timestamp)}` и фильтровать таблицу по выбранному часу |
| **Нет группировки инцидентов** | `events-stats.tsx:149` | Критичные события показываются простым списком, нет группировки по причине/сервису | Проверить `recentCritical`: если есть несколько событий `SERVICE_HEALTH: service_down` для одного сервиса, они показываются отдельно | Реализовать группировку по правилам из раздела 5.1, создать компонент "Incidents" |
| **Нет дедупликации событий** | `events-table.tsx:299-324` | Дубли событий (одинаковые `type`+`action`+`service` в пределах 5 минут) показываются как отдельные строки | Проверить логи: если есть дубли, они показываются все | Добавить дедупликацию: группировать события с одинаковыми ключевыми полями, показывать "[N похожих]" |

### 6.2 UX проблемы

| Проблема | Влияние | Как проверить | Как исправить |
|----------|---------|---------------|---------------|
| **Несогласованные статусы сервисов** | Непонятно, работает ли сервис сейчас (последнее событие может быть "down", но сервис уже восстановлен) | Проверить `SERVICE_HEALTH` события: если последнее = "up", но перед ним было "down", показать текущий статус | Добавить логику: для каждого сервиса брать последнее событие `SERVICE_HEALTH` и показывать его статус в Health Summary |
| **Неоднозначные временные метки** | События показываются в UTC, но пользователь не знает, в каком часовом поясе | Проверить формат `ts` в API: всегда ли UTC? | Добавить отображение часового пояса в header или показывать время в локальном часовом поясе браузера |
| **Повторяющиеся "остановка/запуск"** | Много событий SYSTEM restart подряд — сложно понять, что реально произошло | Проверить логи: есть ли паттерн "restart → restart → restart" | Группировать последовательные restart события в один инцидент "Множественные перезапуски" |
| **Порты/сервисы отображаются непоследовательно** | В разных местах интерфейса порты показываются по-разному (с префиксом "порт" или без) | Проверить `interpretEvent`: как форматируются порты (строка 168, 175) | Унифицировать формат: всегда `сервис (порт N)` или `порт N` |
| **Нет индикации активных инцидентов** | Пользователь не видит, какие проблемы актуальны прямо сейчас | Проверить: есть ли логика определения "активности" события | Добавить проверку: событие активно, если `ts` < 5 минут назад и `severity` = ERROR/WARN |

### 6.3 Технические проблемы

| Проблема | Влияние | Как проверить | Как исправить |
|----------|---------|---------------|---------------|
| **Конфликт версий v2.1 vs 2.0** | В комментариях кода упоминается v2.0, но может быть v2.1 | Проверить `app.py:4`: какая версия указана в комментариях | Обновить комментарии или добавить динамическое отображение версии из API `/api/version` |
| **Нет виртуализации таблицы** | При большом количестве событий (>100) таблица может тормозить | Загрузить страницу с 100+ событиями, проверить производительность скролла | Добавить виртуализацию через `react-window` или пагинацию (50 событий на страницу) |
| **Статистика занимает много места** | 4 большие карточки + блок "По типам" + блок "Критичные" = много вертикального пространства | Открыть страницу, проверить, сколько прокрутки нужно до таблицы | Вынести статистику в компактный sidebar (см. раздел 2) |

---

## 7. Мини-спека для внедрения

### 7.1 Что нужно от API/данных

**Текущее состояние API** (достаточно):
- ✅ `/api/events?limit=100` — список событий
- ✅ `/api/events/stats?hours=24` — статистика + timeline + recentCritical

**Желательные улучшения** (опционально):
- Добавить параметр `hours` в `/api/events` для фильтрации по времени на сервере
- Добавить поле `correlation_id` в события для группировки инцидентов
- Добавить параметр `group_by` для группировки на сервере

**Что можно сделать только в UI**:
- ✅ Группировка инцидентов (эвристика на клиенте)
- ✅ Дедупликация событий
- ✅ Фильтрация по времени на клиенте
- ✅ Интерактивность графика (клик → фильтр таблицы)

### 7.2 Что опасно менять

**Не менять**:
- Структуру API ответов (только расширять)
- Формат событий (типы, поля)
- Логику записи событий в `events.log`

**Можно менять безопасно**:
- UI компоненты (полная переработка)
- Логику фильтрации на клиенте
- Форматирование отображения событий (`interpretEvent`)

### 7.3 План внедрения (поэтапно)

**Этап 1: Рефакторинг layout**
- Переделать страницу на 2-колоночный layout (main + sidebar)
- Вынести статистику в sidebar
- Сделать header sticky

**Этап 2: Улучшение визуализации**
- Переделать график на stacked histogram
- Добавить интерактивность (клик → фильтр)

**Этап 3: Группировка инцидентов**
- Добавить компонент "Критичные события"
- Реализовать логику группировки
- Добавить подсветку активных инцидентов

**Этап 4: Оптимизация таблицы**
- Добавить виртуализацию или пагинацию
- Реализовать дедупликацию
- Улучшить компактность строк

**Этап 5: Исправление багов**
- Исправить фильтрацию по времени
- Унифицировать форматирование
- Добавить индикацию активных инцидентов

---

## 8. Acceptance Criteria

### Функциональные требования

1. ✅ **Sticky Header**: Header остается видимым при прокрутке, содержит переключатели периода и поиск
2. ✅ **Right Sidebar**: Статистика всегда видна в правом сайдбаре (sticky), компактные карточки метрик
3. ✅ **Визуализация 24ч**: График показывает распределение событий по часам с интерактивностью (клик → фильтр таблицы)
4. ✅ **Критичные события**: События ERROR/WARN сгруппированы в инциденты с причиной, длительностью, количеством событий
5. ✅ **Таблица событий**: Компактная таблица с виртуализацией или пагинацией, дефолтная сортировка по времени (новые сверху)
6. ✅ **Фильтрация**: Фильтры типа/уровня применяются ко всем компонентам (график, таблица, статистика)
7. ✅ **Дедупликация**: Дубли событий показываются как свернутые строки с возможностью развернуть
8. ✅ **Активные инциденты**: Инциденты с событиями < 5 минут назад подсвечиваются как активные

### UX требования

9. ✅ **Минимум прокрутки**: Все критичное (статистика, активные инциденты) видно без прокрутки на первом экране
10. ✅ **Быстрая навигация**: Переключение периода — 1 клик, фильтры видны как чипсы
11. ✅ **Информативность**: График показывает распределение по уровням, таблица компактная но читаемая
12. ✅ **Обратная связь**: При клике на график таблица фильтруется, активные фильтры видны в header

### Технические требования

13. ✅ **Производительность**: Виртуализация таблицы при > 50 событиях, оптимизация ре-рендеров
14. ✅ **Адаптивность**: На мобильных устройствах sidebar скрывается, статистика переносится вверх
15. ✅ **Доступность**: Все интерактивные элементы имеют aria-labels, клавиатурная навигация работает

---

## Приложение: Примеры компонентов

### Пример: Right Sidebar — Статистика

```tsx
<div className="w-72 sticky top-16 h-[calc(100vh-4rem)] overflow-auto p-4 space-y-4">
  {/* Метрики */}
  <div className="space-y-2">
    <MetricCard label="Всего" value={stats.total} color="blue" />
    <MetricCard label="Errors" value={stats.errors} color="red" />
    <MetricCard label="Warnings" value={stats.warnings} color="yellow" />
    <MetricCard label="Info" value={stats.info} color="green" />
  </div>
  
  {/* По типам */}
  <div className="space-y-1">
    <h4 className="text-xs font-medium">По типам</h4>
    {Object.entries(stats.byType)
      .sort(([, a], [, b]) => b - a)
      .map(([type, count]) => (
        <Badge key={type} variant="outline" className="text-xs">
          {type}: {count}
        </Badge>
      ))}
  </div>
  
  {/* Health Summary */}
  <div className="space-y-1">
    <h4 className="text-xs font-medium">Health Summary</h4>
    {getServiceHealth().map(service => (
      <div key={service.name} className="flex items-center gap-2 text-xs">
        {service.status === 'UP' ? '✅' : service.status === 'DOWN' ? '🔴' : '⚠️'}
        <span>{service.name}: {service.status}</span>
      </div>
    ))}
  </div>
</div>
```

### Пример: Критичные события (Incidents)

```tsx
<div className="space-y-2">
  <h3 className="text-sm font-medium flex items-center gap-2">
    <AlertCircle className="w-4 h-4 text-red-600" />
    Критичные события
  </h3>
  {incidents.map((incident, idx) => (
    <Collapsible key={idx}>
      <CollapsibleTrigger className="flex items-center justify-between w-full p-3 rounded-lg border bg-muted/50 hover:bg-muted">
        <div className="flex-1 text-left">
          <div className="font-medium text-sm">
            Инцидент #{idx + 1}: {incident.title}
          </div>
          <div className="text-xs text-muted-foreground">
            {formatDuration(incident.duration)} • {incident.eventCount} событий
          </div>
        </div>
        {incident.isActive && (
          <Badge variant="destructive" className="text-xs">Активно</Badge>
        )}
      </CollapsibleTrigger>
      <CollapsibleContent>
        <div className="p-3 space-y-2">
          {incident.events.map(event => (
            <EventRow key={event.ts} event={event} compact />
          ))}
          <Button
            variant="outline"
            size="sm"
            onClick={() => filterByIncident(incident)}
          >
            Перейти к событиям инцидента
          </Button>
        </div>
      </CollapsibleContent>
    </Collapsible>
  ))}
</div>
```

---

**Конец документа**
