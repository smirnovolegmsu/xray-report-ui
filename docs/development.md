# 👨‍💻 Руководство разработчика

## Быстрый старт

### Требования
- Ubuntu 20.04+ / Debian 11+
- Python 3.8+
- Node.js 18+
- Xray-core

### Установка

```bash
# 1. Клонирование репозитория
git clone https://github.com/YOUR_USERNAME/xray-report-ui.git
cd xray-report-ui

# 2. Backend
python3 -m venv venv
source venv/bin/activate
pip install flask python-dateutil

# 3. Frontend
cd frontend
npm install
cd ..
```

---

## Разработка

### Backend (Flask)

```bash
# Активировать виртуальное окружение
source venv/bin/activate

# Запустить сервер разработки
python app.py
```

Backend будет доступен на `http://localhost:8787`

### Frontend (Next.js)

```bash
cd frontend

# Запустить dev сервер
npm run dev

# Или с Turbopack (быстрее, экспериментально)
npm run dev:turbo
```

Frontend будет доступен на `http://localhost:3001` (dev режим)

**Важно:** Production сервер работает на порту 3000, dev сервер на 3001.

---

## Структура проекта

```
/opt/xray-report-ui/
├── app.py                 # Flask Backend
├── frontend/              # Next.js Frontend
│   ├── app/               # Страницы (App Router)
│   ├── components/        # React компоненты
│   ├── lib/               # Утилиты и API клиент
│   └── types/             # TypeScript типы
├── scripts/                # Вспомогательные скрипты
└── docs/                  # Документация
```

---

## API разработка

### Backend API

Все эндпоинты находятся в `app.py`:

- `/api/users` — управление пользователями
- `/api/dashboard` — данные дашборда
- `/api/live/*` — мониторинг в реальном времени
- `/api/settings` — настройки системы
- `/api/system/*` — управление системой

### Frontend API клиент

API клиент находится в `frontend/lib/api.ts`:

```typescript
import { api } from '@/lib/api';

// Получить список пользователей
const users = await api.users.getAll();

// Добавить пользователя
await api.users.add({ uuid, email, ... });

// Получить статистику дашборда
const dashboard = await api.dashboard.get();
```

---

## Стиль кода

### Python (Backend)
- PEP 8 стиль
- Type hints где возможно
- Docstrings для функций

### TypeScript/React (Frontend)
- ESLint конфигурация
- TypeScript strict mode
- Компоненты в PascalCase
- Файлы в kebab-case

---

## Тестирование

### Backend
```bash
# Запустить тесты (если есть)
pytest
```

### Frontend
```bash
cd frontend

# Проверка типов
npm run type-check

# Линтинг
npm run lint
```

---

## Сборка

### Frontend Production сборка

```bash
cd frontend

# Полная сборка (с проверками)
npm run build

# Быстрая сборка (без проверок)
./build-fast.sh
```

### Оптимизация сборки

См. [документацию по оптимизации](./optimization.md).

---

## Отладка

### Backend логи
```bash
# Логи в реальном времени
journalctl -u xray-report-ui -f

# Последние 50 строк
journalctl -u xray-report-ui -n 50
```

### Frontend логи
```bash
# Логи в реальном времени
journalctl -u xray-nextjs-ui -f

# Последние 50 строк
journalctl -u xray-nextjs-ui -n 50
```

### Dev режим
В dev режиме логи выводятся в консоль.

---

## Git workflow

### Ветки
- `main` — production код
- `feat/*` — новые функции
- `fix/*` — исправления багов
- `docs/*` — документация

### Коммиты
Используйте понятные сообщения коммитов:
```
feat: добавить фильтр по датам на странице событий
fix: исправить ошибку при добавлении пользователя
docs: обновить документацию по портам
```

---

## Полезные команды

### Проверка статуса сервисов
```bash
systemctl status xray-nextjs-ui xray-report-ui
```

### Перезапуск сервисов
```bash
systemctl restart xray-nextjs-ui xray-report-ui
```

### Анализ портов
```bash
./scripts/ports/analyze.sh
```

### Проверка сервисов
```bash
./scripts/services/check.sh
```

---

## Ресурсы

- [Документация по архитектуре](./architecture.md)
- [Документация по портам](./ports.md)
- [Документация по сервисам](./services.md)
- [Оптимизация и производительность](./optimization.md)
- [Решение проблем](./troubleshooting.md)

---

## FAQ

### Как добавить новую страницу?
1. Создайте файл в `frontend/app/your-page/page.tsx`
2. Добавьте ссылку в `frontend/components/layout/sidebar.tsx`

### Как добавить новый API эндпоинт?
1. Добавьте роут в `app.py`
2. Добавьте метод в `frontend/lib/api.ts`
3. Используйте в компонентах через `api.yourModule.method()`

### Как обновить зависимости?
```bash
# Backend
source venv/bin/activate
pip install -r requirements.txt

# Frontend
cd frontend
npm install
```
