# 🔌 Документация по портам

## 📊 Текущее состояние портов

На сервере открыто **10 портов**, из которых:
- **7 критичных** - необходимы для работы системы и приложения
- **3 порта Cursor Server** - для работы Cursor через SSH (слушают только localhost)

---

## 🔐 Критичные порты (ОСТАВИТЬ)

### Порт 22 - SSH
- **Процесс:** `sshd`
- **Назначение:** Удаленный доступ к серверу по протоколу SSH
- **Доступ:** Публичный (0.0.0.0)
- **Статус:** ✅ **ОСТАВИТЬ** - необходим для управления сервером
- **Firewall:** Разрешен в UFW
- **Можно ли закрыть:** ❌ НЕТ - без него не будет доступа к серверу

### Порт 53 - DNS
- **Процесс:** `systemd-resolved`
- **Назначение:** Разрешение доменных имен (локальный DNS кэш)
- **Доступ:** Только localhost (127.0.0.53)
- **Статус:** ✅ **ОСТАВИТЬ** - системный сервис
- **Firewall:** Не требуется (только localhost)
- **Можно ли закрыть:** ❌ НЕТ - критично для работы системы и приложений

### Порт 80 - HTTP
- **Процесс:** `nginx`
- **Назначение:** Веб-сервер HTTP
- **Доступ:** Публичный (0.0.0.0)
- **Статус:** ✅ **ОСТАВИТЬ** - необходим для веб-доступа
- **Firewall:** Разрешен в UFW
- **Можно ли закрыть:** ❌ НЕТ - необходим для веб-доступа

### Порт 443 - Xray VPN
- **Процесс:** `xray`
- **Назначение:** VPN сервер для клиентов (VLESS + Reality)
- **Доступ:** Публичный (0.0.0.0)
- **Статус:** ✅ **ОСТАВИТЬ** - основной VPN порт
- **Firewall:** Разрешен в UFW
- **Протокол:** VLESS + Reality (маскируется под обычный HTTPS трафик)
- **Можно ли закрыть:** ❌ НЕТ - это основной VPN порт, без него VPN не будет работать

### Порт 3000 - Next.js Frontend
- **Процесс:** `next-server`
- **Назначение:** Веб-интерфейс панели управления Xray Report UI
- **Доступ:** Публичный (0.0.0.0)
- **Статус:** ✅ **ОСТАВИТЬ** - основной UI приложения
- **Firewall:** Разрешен в UFW
- **URL:** `http://YOUR_SERVER_IP:3000`
- **Можно ли закрыть:** ❌ НЕТ - это основной UI приложения

### Порт 8787 - Flask Backend API
- **Процесс:** `python` (Flask)
- **Назначение:** REST API для управления Xray и пользователями
- **Доступ:** Только localhost (127.0.0.1)
- **Статус:** ✅ **ОСТАВИТЬ** - необходим для работы приложения
- **Firewall:** Не требуется (только localhost)
- **Эндпоинты:** `/api/users`, `/api/settings`, `/api/dashboard`, etc.
- **Можно ли закрыть:** ❌ НЕТ - без него фронтенд не будет работать

### Порт 10085 - Xray API
- **Процесс:** `xray`
- **Назначение:** Внутренний API для управления Xray (Stats API)
- **Доступ:** Только localhost (127.0.0.1)
- **Статус:** ✅ **ОСТАВИТЬ** - необходим для управления Xray
- **Firewall:** Не требуется (только localhost)
- **Можно ли закрыть:** ❌ НЕТ - без него панель управления не сможет получать статистику

---

## 💻 Порты Cursor Server (ОПЦИОНАЛЬНО)

Эти порты используются для работы Cursor IDE через SSH. Они слушают **только на localhost (127.0.0.1)**, поэтому безопасны и не доступны извне.

### Порт 37011 - Cursor Server Main
- **Процесс:** `node` (Cursor Server Main)
- **Назначение:** Основной сервер Cursor для работы через SSH
- **Доступ:** Только localhost (127.0.0.1)
- **Статус:** ⚠️ **ОПЦИОНАЛЬНО** - можно закрыть, если не используете Cursor через SSH
- **Рекомендация:** Оставить, если используете Cursor для разработки

### Порт 40783 - Cursor Extension Host
- **Процесс:** `node` (Cursor Extension Host)
- **Назначение:** Хост расширений Cursor
- **Доступ:** Только localhost (127.0.0.1)
- **Статус:** ⚠️ **ОПЦИОНАЛЬНО** - можно закрыть, если не используете Cursor через SSH
- **Рекомендация:** Оставить, если используете Cursor для разработки

### Порт 46705 - Cursor Multiplex Server
- **Процесс:** `node` (Cursor Multiplex Server)
- **Назначение:** Мультиплексный сервер Cursor
- **Доступ:** Только localhost (127.0.0.1)
- **Статус:** ⚠️ **ОПЦИОНАЛЬНО** - можно закрыть, если не используете Cursor через SSH
- **Рекомендация:** Оставить, если используете Cursor для разработки

---

## 📋 Сводная таблица

| Порт | Протокол | Процесс | Назначение | Доступ | Критичность |
|------|----------|---------|------------|--------|-------------|
| 22 | TCP | sshd | SSH доступ | Публичный | 🔴 Критичный |
| 53 | TCP/UDP | systemd-resolved | DNS | localhost | 🔴 Критичный |
| 80 | TCP | nginx | HTTP | Публичный | 🔴 Критичный |
| 443 | TCP | xray | Xray VPN | Публичный | 🔴 Критичный |
| 3000 | TCP | next-server | Next.js UI | Публичный | 🔴 Критичный |
| 8787 | TCP | python | Flask API | localhost | 🔴 Критичный |
| 10085 | TCP | xray | Xray API | localhost | 🔴 Критичный |
| 37011 | TCP | node | Cursor Server | localhost | 🟡 Опциональный |
| 40783 | TCP | node | Cursor Extension | localhost | 🟡 Опциональный |
| 46705 | TCP | node | Cursor Multiplex | localhost | 🟡 Опциональный |

---

## 🔧 Управление портами

### Анализ портов
```bash
# Автоматический анализ всех портов
./scripts/ports/analyze.sh

# Ручной просмотр всех открытых портов
ss -tulpn | grep LISTEN

# Детальная информация о конкретном порте
lsof -i :PORT
```

### Очистка портов
```bash
# Просмотр, что будет закрыто (безопасно, без изменений)
./scripts/ports/cleanup.sh --dry-run

# Закрыть неиспользуемые порты
./scripts/ports/cleanup.sh

# Закрыть порты Cursor Server (если не используете)
./scripts/ports/cleanup.sh --kill-cursor
```

### Оптимизация портов
```bash
# Интерактивная оптимизация с рекомендациями
./scripts/ports/optimize.sh
```

---

## 🛡️ Безопасность

### Публичные порты (доступны извне)
- **22** (SSH) - защищен паролем/ключом
- **80** (HTTP) - защищен firewall
- **3000** (Next.js) - защищен firewall
- **443** (VPN) - защищен firewall

### Локальные порты (только localhost)
- **53** (DNS) - системный сервис
- **8787** (Backend) - только для внутреннего использования
- **10085** (Xray API) - только для внутреннего использования
- **37011, 40783, 46705** (Cursor) - только для SSH-туннеля

**Вывод:** Все порты настроены правильно и безопасно. Порты Cursor Server не представляют угрозы, так как недоступны извне.

---

## 🔍 Полезные команды

### Проверка порта
```bash
# Проверить, слушает ли порт
ss -tulpn | grep :PORT

# Детальная информация о порте
lsof -i :PORT

# Проверить процесс
ps aux | grep PID
```

### Проверка firewall
```bash
sudo ufw status numbered
```

---

## ✅ Выводы и рекомендации

1. **Все критичные порты (7 штук) должны оставаться открытыми** - они необходимы для работы системы и приложения.

2. **Порты Cursor Server (3 штуки) можно закрыть**, если вы не используете Cursor через SSH. Они безопасны, но если не нужны - закройте.

3. **Firewall настроен правильно** - все публичные порты защищены правилами UFW.

4. **Оптимизация:** Максимум можно закрыть 3 порта (Cursor Server), если они не нужны. Это не критично, так как они слушают только на localhost.

---

## 📝 Примечания

- Порты, слушающие на `127.0.0.1` (localhost), **недоступны извне** и безопасны
- Порты, слушающие на `0.0.0.0` или `*`, **доступны извне** и должны быть защищены firewall
- Все публичные порты (22, 80, 3000, 443) правильно настроены в UFW
- Порты Cursor Server автоматически закрываются при отключении SSH-сессии
