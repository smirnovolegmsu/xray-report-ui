# План реализации редизайна страницы "События"

## Приоритизация задач

### 🔴 Критичные баги (исправить в первую очередь)
1. ✅ `timeRange` не передается в `EventsTable` → Таблица игнорирует выбранный период
2. ✅ Фильтры не применяются к графику → График показывает все события
3. ✅ Таблица использует `key={idx}` → Проблемы с ре-рендерингом
4. ✅ График не интерактивен → Нет возможности фильтровать по клику

### 🟡 UX улучшения (второй приоритет)
5. ✅ Рефакторинг layout → 2-колоночный с sidebar
6. ✅ Компактная статистика в sidebar
7. ✅ Группировка инцидентов
8. ✅ Дедупликация событий

### 🟢 Дополнительные улучшения (третий приоритет)
9. ✅ Виртуализация таблицы
10. ✅ Индикация активных инцидентов
11. ✅ Улучшенная визуализация 24ч

---

## Этап 1: Исправление критичных багов

### Задача 1.1: Передача `timeRange` в `EventsTable`
**Файлы**: `frontend/app/events/page.tsx`, `frontend/components/features/events/events-table.tsx`

**Изменения**:
- Добавить `timeRange` в props `EventsTable`
- Добавить фильтрацию событий по времени на клиенте

### Задача 1.2: Применение фильтров к графику
**Файлы**: `frontend/app/events/page.tsx`, `frontend/components/features/events/events-timeline.tsx`

**Изменения**:
- Передать `typeFilter` и `severityFilter` в `EventsTimeline`
- Фильтровать `timeline` данные на клиенте

### Задача 1.3: Исправление ключей в таблице
**Файлы**: `frontend/components/features/events/events-table.tsx`

**Изменения**:
- Заменить `key={idx}` на уникальный ключ

### Задача 1.4: Интерактивность графика
**Файлы**: `frontend/components/features/events/events-timeline.tsx`, `frontend/app/events/page.tsx`

**Изменения**:
- Добавить обработчик клика на столбцы графика
- Добавить состояние `selectedHour` в родительский компонент
- Фильтровать таблицу по выбранному часу

---

## Этап 2: Рефакторинг layout

### Задача 2.1: Sticky Header
**Файлы**: `frontend/app/events/page.tsx`

**Изменения**:
- Сделать header sticky
- Переместить переключатели периода и поиск в header
- Добавить фильтры-чипсы для активных фильтров

### Задача 2.2: Right Sidebar со статистикой
**Файлы**: `frontend/app/events/page.tsx`, `frontend/components/features/events/events-stats.tsx`

**Изменения**:
- Создать 2-колоночный layout (main + sidebar)
- Переделать `EventsStats` в компактный sidebar компонент
- Сделать sidebar sticky

### Задача 2.3: Реорганизация main content
**Файлы**: `frontend/app/events/page.tsx`

**Изменения**:
- Упорядочить: График → Критичные события → Таблица
- Убрать лишние отступы

---

## Этап 3: Группировка инцидентов

### Задача 3.1: Компонент "Критичные события"
**Файлы**: `frontend/components/features/events/events-incidents.tsx` (новый)

**Функционал**:
- Группировка событий по инцидентам
- Collapsible карточки
- Подсветка активных инцидентов

### Задача 3.2: Логика группировки
**Файлы**: `frontend/lib/events-utils.ts` (новый)

**Функционал**:
- Функция `groupEventsIntoIncidents(events: Event[]): Incident[]`
- Правила группировки (correlation_id или эвристика)

---

## Этап 4: Дедупликация и оптимизация таблицы

### Задача 4.1: Дедупликация событий
**Файлы**: `frontend/components/features/events/events-table.tsx`

**Функционал**:
- Группировка дублей
- Показ "[N похожих]" с возможностью развернуть

### Задача 4.2: Виртуализация таблицы
**Файлы**: `frontend/components/features/events/events-table.tsx`

**Функционал**:
- Добавить `react-window` или пагинацию
- Оптимизация рендеринга при большом количестве событий

---

## Этап 5: Улучшение визуализации

### Задача 5.1: Stacked Histogram
**Файлы**: `frontend/components/features/events/events-timeline.tsx`

**Функционал**:
- Переделать на stacked histogram (recharts)
- Показ распределения по уровням (INFO/WARN/ERROR)

### Задача 5.2: Интерактивность
**Функционал**:
- Клик по столбцу → фильтрация таблицы
- Hover → tooltip с деталями

---

## Чеклист для проверки

### После Этапа 1:
- [ ] Таблица фильтруется по выбранному периоду
- [ ] График фильтруется по типу/уровню
- [ ] Клик по графику фильтрует таблицу
- [ ] Нет проблем с ре-рендерингом таблицы

### После Этапа 2:
- [ ] Header sticky и содержит все фильтры
- [ ] Статистика в компактном sidebar
- [ ] Минимум прокрутки до критичной информации

### После Этапа 3:
- [ ] Критичные события сгруппированы в инциденты
- [ ] Активные инциденты подсвечены
- [ ] Можно развернуть инцидент для деталей

### После Этапа 4:
- [ ] Дубли событий свернуты
- [ ] Таблица работает быстро при 100+ событиях

### После Этапа 5:
- [ ] График показывает распределение по уровням
- [ ] График интерактивен

---

## Порядок выполнения

1. **Этап 1** (критичные баги) → сразу
2. **Этап 2** (layout) → после Этапа 1
3. **Этап 3** (инциденты) → параллельно с Этапом 2
4. **Этап 4** (оптимизация) → после Этапа 2-3
5. **Этап 5** (визуализация) → финальный этап
