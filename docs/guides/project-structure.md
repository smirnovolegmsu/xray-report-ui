# Подробное описание структуры проекта Xray Report UI

## Обзор проекта

**Xray Report UI** — это веб-панель управления VPN сервером на базе Xray-core. Проект состоит из двух основных компонентов:
- **Backend (Flask API)** — Python сервер на порту 8787
- **Frontend (Next.js)** — React приложение на порту 3000

---

## 📁 Структура корневой директории

### 🔧 Основные Python файлы (Backend)

#### `app.py`
**Назначение:** Главный файл Flask приложения, точка входа для Backend сервера.

**Что делает:**
- Инициализирует Flask приложение
- Регистрирует все API Blueprints (маршруты)
- Запускает фоновые потоки для:
  - Обновления live buffer (каждую минуту)
  - Проверки здоровья сервисов (каждые 60 секунд)
- Обрабатывает ошибки и логирует их как события
- Управляет жизненным циклом приложения (startup/shutdown)

**Важно:** При запуске в production используется Gunicorn, который вызывает этот файл.

---

#### `config.py`
**Назначение:** Файл конфигурации с константами и настройками по умолчанию.

**Содержит:**
- Имена systemd сервисов (`SERVICE_UI`, `SERVICE_XRAY_DEFAULT`, `SERVICE_NEXTJS`)
- Порты (`APP_PORT=8787`, `NEXTJS_PORT=3000`)
- Пути к файлам Xray (конфиг, логи, usage директория)
- Настройки кэширования (TTL для разных типов данных)
- Размеры буферов и лимиты

**Использование:** Импортируется во всех модулях для доступа к константам.

---

#### `utils.py`
**Назначение:** Утилитарные функции общего назначения.

**Функции:**
- `now_utc_iso()` — получение текущего UTC времени в ISO формате
- `atomic_write_text()` — атомарная запись текста в файл (безопасная)
- `atomic_write_json()` — атомарная запись JSON объекта
- `read_json()` — безопасное чтение JSON с fallback на default значение
- `parse_date_from_filename()` — парсинг даты из имени файла (например, `usage_2024-01-15.csv`)

**Использование:** Используется во всех модулях для работы с файлами и данными.

---

#### `cache.py`
**Назначение:** Система кэширования для оптимизации производительности.

**Что делает:**
- Кэширует результаты тяжелых операций (парсинг логов, статистика)
- Управляет TTL (время жизни кэша) для разных типов данных
- Автоматически очищает старые записи при превышении лимита
- Используется для dashboard, usage, live данных, пользователей

**Зачем:** Уменьшает нагрузку на CPU и ускоряет ответы API.

---

#### `xray.py`
**Назначение:** Модуль для работы с Xray-core конфигурацией.

**Что делает:**
- Читает конфигурационный файл Xray (`/usr/local/etc/xray/config.json`)
- Парсит пользователей (clients) из конфига
- Генерирует VLESS ссылки для пользователей
- Управляет пользователями (добавление, удаление, обновление)
- Сохраняет изменения обратно в конфиг

**Важно:** Все изменения конфига Xray проходят через этот модуль.

---

#### `users.py`
**Назначение:** Бизнес-логика для работы с пользователями.

**Что делает:**
- Валидация данных пользователей
- Формирование статистики по пользователям
- Расчет трафика и активности
- Интеграция с `xray.py` для управления пользователями

**Использование:** Используется API модулем `api/users.py`.

---

#### `events.py`
**Назначение:** Система событий и логирования.

**Что делает:**
- Записывает события в файл `data/events.log`
- Типы событий: `SYSTEM`, `USER_ACTION`, `SERVICE_HEALTH`, `APP_ERROR` и др.
- Уровни серьезности: `INFO`, `WARN`, `ERROR`
- Поддержка фильтрации и поиска событий

**Зачем:** Отслеживание всех важных действий в системе для отладки и мониторинга.

---

#### `live.py`
**Назначение:** Модуль для live мониторинга подключений в реальном времени.

**Что делает:**
- Парсит access log Xray для определения активных подключений
- Поддерживает буфер на 24 часа (1440 точек по минутам)
- Определяет "онлайн" статус пользователей (подключения за последние 5 минут)
- Сохраняет/загружает буфер из файла для восстановления после перезапуска

**Использование:** Используется страницей `/online` и API `/api/live/*`.

---

#### `dashboard.py`
**Назначение:** Модуль для формирования данных дашборда (главной страницы).

**Что делает:**
- Агрегирует статистику по пользователям
- Рассчитывает общий трафик за период
- Формирует топ доменов
- Подготавливает данные для графиков

**Использование:** Используется API `/api/dashboard` и главной страницей Frontend.

---

#### `settings.py`
**Назначение:** Управление настройками приложения.

**Что делает:**
- Загружает настройки из `data/settings.json`
- Инициализирует директории (`data/`, `data/backups/`)
- Поддерживает настройки путей, коллектора, UI параметров
- Сохраняет изменения настроек атомарно

**Важно:** При первом запуске создает файл настроек с дефолтными значениями.

---

#### `backups.py`
**Назначение:** Система резервного копирования конфигурации Xray.

**Что делает:**
- Создает бэкапы конфига Xray перед изменениями
- Хранит бэкапы в `data/backups/`
- Поддерживает восстановление из бэкапа
- Автоматическая очистка старых бэкапов

**Зачем:** Безопасность — возможность откатить изменения конфига.

---

#### `system.py`
**Назначение:** Работа с системными ресурсами и сервисами.

**Что делает:**
- Проверка статуса systemd сервисов (xray, xray-report-ui, xray-nextjs-ui)
- Мониторинг системных ресурсов (CPU, RAM, диск)
- Управление портами (проверка занятости)
- Сохранение статистики Xray перед перезапуском

**Использование:** Используется API `/api/system/*` и компонентами Frontend.

---

#### `collector.py`
**Назначение:** Модуль для сбора и парсинга статистики использования из CSV файлов.

**Что делает:**
- Парсит CSV файлы из `/var/log/xray/usage/` (формат: `usage_YYYY-MM-DD.csv`)
- Агрегирует данные по пользователям и датам
- Поддерживает фильтрацию по периодам
- Кэширует результаты для производительности

**Использование:** Используется для отображения статистики трафика на дашборде.

---

### 📂 Папка `api/` — API Blueprints (Flask маршруты)

**Назначение:** Модульная структура API endpoints. Каждый файл — отдельный Blueprint с группой связанных маршрутов.

#### `api/__init__.py`
**Назначение:** Общие утилиты для API (функции `ok()`, `fail()`, валидация).

**Функции:**
- `ok(data)` — формирование успешного JSON ответа
- `fail(error, code)` — формирование ошибки JSON ответа
- `validate_email()` — валидация email
- `validate_uuid()` — валидация UUID v4
- `validate_filename()` — валидация имени файла (защита от path traversal)

---

#### `api/users.py`
**Назначение:** API endpoints для управления пользователями.

**Маршруты:**
- `GET /api/users` — список всех пользователей
- `POST /api/users/add` — добавить пользователя
- `POST /api/users/delete` — удалить пользователя
- `GET /api/users/stats` — статистика по пользователям
- `GET /api/users/link?uuid=...` — получить VLESS ссылку

**Использование:** Страница `/users` в Frontend.

---

#### `api/dashboard.py`
**Назначение:** API endpoints для дашборда (главной страницы).

**Маршруты:**
- `GET /api/dashboard` — данные дашборда (статистика, графики)
- `GET /api/usage/dashboard` — статистика использования за период

**Использование:** Главная страница `/` в Frontend.

---

#### `api/live.py`
**Назначение:** API endpoints для live мониторинга.

**Маршруты:**
- `GET /api/live/now` — текущие подключения (кто онлайн)
- `GET /api/live/series` — временные ряды для графиков

**Использование:** Страница `/online` в Frontend.

---

#### `api/events.py`
**Назначение:** API endpoints для работы с событиями.

**Маршруты:**
- `GET /api/events` — список событий с фильтрацией
- `GET /api/events/stats` — статистика по событиям

**Использование:** Страница `/events` в Frontend.

---

#### `api/system.py`
**Назначение:** API endpoints для системной информации.

**Маршруты:**
- `GET /api/system/status` — статус системы и сервисов
- `POST /api/system/restart` — перезапуск сервисов
- `GET /api/ports/status` — статус портов

**Использование:** Компоненты системной информации в Frontend.

---

#### `api/settings.py`
**Назначение:** API endpoints для настроек.

**Маршруты:**
- `GET /api/settings` — получить настройки
- `POST /api/settings` — сохранить настройки

**Использование:** Страница `/settings` в Frontend.

---

#### `api/backups.py`
**Назначение:** API endpoints для управления бэкапами.

**Маршруты:**
- `GET /api/backups` — список бэкапов
- `POST /api/backups/create` — создать бэкап
- `POST /api/backups/restore` — восстановить из бэкапа
- `DELETE /api/backups/<id>` — удалить бэкап

**Использование:** Раздел Backups на странице `/settings`.

---

#### `api/xray.py`
**Назначение:** API endpoints для работы с конфигом Xray.

**Маршруты:**
- `GET /api/xray/config` — просмотр конфига Xray
- `GET /api/xray/reload` — перезагрузить конфиг Xray

**Использование:** Раздел Xray Config на странице `/settings`.

---

#### `api/collector.py`
**Назначение:** API endpoints для работы с коллектором статистики.

**Маршруты:**
- `GET /api/collector/status` — статус коллектора
- `POST /api/collector/run` — запустить сбор статистики

**Использование:** Раздел Collector на странице `/settings`.

---

#### `api/common.py`
**Назначение:** Общие API endpoints (ping, health check).

**Маршруты:**
- `GET /api/ping` — проверка доступности API

**Использование:** Health checks и мониторинг.

---

#### `api/tests.py`
**Назначение:** API endpoints для тестирования (development/debugging).

**Маршруты:**
- Различные тестовые endpoints для отладки

**Использование:** Только для разработки и тестирования.

---

### 📂 Папка `frontend/` — Next.js Frontend приложение

**Назначение:** Современный React UI на Next.js 14+ с App Router.

#### Структура `frontend/`:

##### `frontend/app/` — Next.js App Router страницы
- `app/page.tsx` — главная страница (Dashboard/Overview)
- `app/layout.tsx` — корневой layout (обертка для всех страниц)
- `app/globals.css` — глобальные стили
- `app/users/page.tsx` — страница управления пользователями
- `app/online/page.tsx` — страница live мониторинга
- `app/events/page.tsx` — страница событий
- `app/settings/page.tsx` — страница настроек
- `app/favicon.ico` — иконка сайта

##### `frontend/components/` — React компоненты

**`components/layout/`** — компоненты макета:
- `main-layout.tsx` — основной layout с sidebar и header
- `sidebar.tsx` — боковое меню навигации
- `header.tsx` — верхний заголовок
- `status-badges.tsx` — бейджи статуса сервисов
- `system-resources.tsx` — отображение системных ресурсов
- `ports-status.tsx` — статус портов
- `viewport-toggle.tsx` — переключатель вида (мобильный/десктоп)

**`components/features/`** — функциональные компоненты:

- **`overview/`** — компоненты главной страницы:
  - `metrics-cards.tsx` — карточки метрик
  - `traffic-chart.tsx` — график трафика
  - `top-domains.tsx` — топ доменов
  - `user-stats-cards.tsx` — статистика пользователей
  - `date-selector.tsx` — выбор даты
  - `metric-filter.tsx` — фильтр метрик
  - `mode-toggle.tsx` — переключатель режима отображения

- **`users/`** — компоненты управления пользователями:
  - `users-table.tsx` — таблица пользователей
  - `add-user-dialog.tsx` — диалог добавления пользователя
  - `edit-alias-dialog.tsx` — диалог редактирования алиаса
  - `user-details-sheet.tsx` — детальная информация о пользователе
  - `user-link-dialog.tsx` — диалог получения VLESS ссылки

- **`live/`** — компоненты live мониторинга:
  - `live-now.tsx` — текущие подключения
  - `live-charts.tsx` — графики активности
  - `live-controls.tsx` — элементы управления

- **`events/`** — компоненты событий:
  - `events-table.tsx` — таблица событий
  - `events-timeline.tsx` — временная линия событий
  - `events-stats-sidebar.tsx` — боковая панель статистики
  - `events-critical-alerts.tsx` — критические алерты
  - `events-problematic-services.tsx` — проблемные сервисы
  - `events-trend-and-repeated.tsx` — тренды и повторяющиеся события
  - `event-icon.tsx` — иконки типов событий
  - `service-icon.tsx` — иконки сервисов

- **`settings/`** — компоненты настроек:
  - `backups-settings.tsx` — управление бэкапами
  - `collector-settings.tsx` — настройки коллектора
  - `ports-settings.tsx` — настройки портов
  - `system-settings.tsx` — системные настройки
  - `xray-settings.tsx` — настройки Xray
  - `xray-config-viewer.tsx` — просмотр конфига Xray
  - `tests-settings.tsx` — тестовые настройки
  - `journal-viewer.tsx` — просмотр systemd journal

**`components/ui/`** — базовые UI компоненты (shadcn/ui):
- `button.tsx`, `card.tsx`, `dialog.tsx`, `table.tsx` и др.
- Переиспользуемые компоненты интерфейса

**`components/providers.tsx`** — React провайдеры (контексты)

##### `frontend/lib/` — утилиты и библиотеки

- `api.ts` — клиент для работы с Backend API
- `time-utils.ts` — утилиты для работы со временем
- `utils.ts` — общие утилиты (cn, форматирование)
- `store.ts` — глобальное состояние (Zustand)
- `i18n.ts` — интернационализация (RU/EN)
- `card-colors.ts` — цвета для карточек
- `card-sizes.ts` — размеры карточек
- `resource-colors.ts` — цвета для ресурсов
- `framer-variants.ts` — анимации (Framer Motion)
- `number-flow-config.ts` — конфигурация анимации чисел
- `hooks/` — кастомные React хуки:
  - `use-dashboard-data.ts` — данные дашборда
  - `use-events-data.ts` — данные событий
  - `use-debounce.ts` — debounce для поиска
  - `use-is-mobile.ts` — определение мобильного устройства

##### `frontend/types/` — TypeScript типы

- `index.ts` — все типы данных (User, Event, DashboardData и др.)

##### `frontend/public/` — статические файлы

- SVG иконки и изображения

##### Конфигурационные файлы Frontend:

- `package.json` — зависимости Node.js и скрипты
- `next.config.ts` — конфигурация Next.js
- `tsconfig.json` — конфигурация TypeScript
- `components.json` — конфигурация shadcn/ui
- `postcss.config.mjs` — конфигурация PostCSS
- `eslint.config.mjs` — конфигурация ESLint
- `.npmrc` — настройки npm
- `build-fast.sh` — скрипт быстрой сборки

---

### 📂 Папка `tests/` — Тесты (pytest)

**Назначение:** Unit и интеграционные тесты для Backend.

#### `tests/conftest.py`
**Назначение:** Конфигурация pytest (fixtures, настройки).

#### `tests/test_users.py`
**Назначение:** Тесты для модуля `users.py` и API `api/users.py`.

#### `tests/test_events.py`
**Назначение:** Тесты для модуля `events.py` и API `api/events.py`.

#### `tests/test_settings.py`
**Назначение:** Тесты для модуля `settings.py` и API `api/settings.py`.

#### `tests/test_backups.py`
**Назначение:** Тесты для модуля `backups.py` и API `api/backups.py`.

#### `tests/test_xray.py`
**Назначение:** Тесты для модуля `xray.py`.

#### `tests/test_utils.py`
**Назначение:** Тесты для утилит `utils.py`.

#### `tests/test_api_validation.py`
**Назначение:** Тесты валидации API (email, UUID, filename).

#### `tests/__init__.py`
**Назначение:** Инициализация пакета тестов.

#### `pytest.ini`
**Назначение:** Конфигурация pytest (пути, опции).

---

### 📂 Папка `scripts/` — Вспомогательные скрипты

**Назначение:** Bash скрипты для управления и обслуживания проекта.

#### `scripts/ports/` — скрипты для работы с портами:
- `analyze.sh` — анализ использования портов
- `cleanup.sh` — очистка неиспользуемых портов
- `optimize.sh` — оптимизация портов

#### `scripts/services/` — скрипты для работы с сервисами:
- `check.sh` — проверка статуса всех сервисов
- `health-check.sh` — проверка здоровья сервисов

#### Другие скрипты:
- `check-build-id.sh` — проверка ID сборки Frontend
- `check-events-layout.sh` — проверка layout страницы событий
- `optimize-cpu.sh` — оптимизация использования CPU
- `verify-mobile-header-fix.sh` — проверка исправления мобильного header

#### `scripts/README.md`
**Назначение:** Документация по использованию скриптов.

---

### 📂 Папка `systemd/` — Systemd сервисы

**Назначение:** Конфигурационные файлы для запуска приложения как systemd сервисов.

#### `systemd/xray-report-ui.service`
**Назначение:** Systemd сервис для Backend (Flask API на порту 8787).

**Что делает:**
- Запускает Gunicorn с Flask приложением
- Автоматический перезапуск при сбоях
- Логирование в systemd journal

#### `systemd/xray-nextjs-ui.service`
**Назначение:** Systemd сервис для Frontend (Next.js на порту 3000).

**Что делает:**
- Запускает Next.js production сервер
- Автоматический перезапуск при сбоях
- Логирование в systemd journal

**Использование:**
```bash
sudo cp systemd/xray-report-ui.service /etc/systemd/system/
sudo cp systemd/xray-nextjs-ui.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now xray-report-ui xray-nextjs-ui
```

---

### 📂 Папка `docs/` — Документация

**Назначение:** Вся документация проекта (архитектура, оптимизация, багфиксы, case studies).

#### Основные документы:
- `README.md` — общий README (в корне проекта)
- `architecture.md` — описание архитектуры системы
- `structure.md` — структура проекта
- `ports.md` — документация по портам
- `services.md` — документация по сервисам
- `optimization.md` — оптимизация и производительность
- `development.md` — руководство разработчика
- `troubleshooting.md` — решение проблем
- `monitoring.md` — мониторинг системы

#### Аудит и отчеты:
- `ARCHITECTURE_AUDIT.md` — аудит архитектуры
- `ARCHITECTURE_AUDIT_FULL.md` — полный аудит архитектуры
- `CODE_AUDIT_REPORT.md` — аудит кода
- `FINAL_AUDIT_REPORT.md` — финальный отчет аудита
- `MODULE_AUDIT_*.md` — аудиты отдельных модулей

#### Оптимизация:
- `API_OPTIMIZATION.md` — оптимизация API
- `OPTIMIZATION_PLAN.md` — план оптимизации
- `OPTIMIZATION_SUMMARY.md` — итоги оптимизации
- `FINAL_OPTIMIZATION_REPORT.md` — финальный отчет оптимизации

#### Багфиксы:
- `BUGFIXES_SUMMARY.md` — итоги исправлений багов
- `BUGFIXES_CPU_RAM_ONLINE.md` — исправления CPU/RAM на странице Online
- `HYDRATION_ERROR_418_FIX.md` — исправление ошибки гидратации
- `CHUNK_LOAD_ERROR_FIX.md` — исправление ошибки загрузки chunk
- `EVENTS_NAVIGATION_FIX.md` — исправление навигации событий

#### Рефакторинг:
- `REFACTORING_PHASE1_COMPLETED.md` — завершение фазы 1 рефакторинга
- `REFACTORING_PHASE2_COMPLETED.md` — завершение фазы 2 рефакторинга
- `REFACTORING_PHASE3_COMPLETED.md` — завершение фазы 3 рефакторинга
- `EVENTS_REFACTORING_README.md` — рефакторинг событий
- `events-refactoring-*.md` — различные документы по рефакторингу событий

#### Case Studies:
- `CASE_STUDIES.md` — кейсы и примеры использования
- `README_CASE_STUDIES.md` — README по case studies

#### Другие документы:
- `ERROR_LOG.md` — лог ошибок
- `STABILITY_IMPROVEMENTS.md` — улучшения стабильности
- `QUICK_WINS_COMPLETED.md` — быстрые улучшения
- `AUDIT_FIXES_APPLIED.md` — примененные исправления аудита
- И множество других документов по различным аспектам проекта

---

### 📄 Корневые конфигурационные файлы

#### `.gitignore`
**Назначение:** Список файлов и папок, которые Git должен игнорировать.

**Игнорирует:**
- `venv/` — Python virtual environment
- `data/` — данные приложения (настройки, логи, бэкапы)
- `frontend/.next/` — build output Next.js
- `frontend/node_modules/` — зависимости Node.js
- `__pycache__/`, `*.pyc` — Python кэш
- IDE файлы (`.vscode/`, `.idea/`)
- Логи (`*.log`)

---

#### `requirements.txt`
**Назначение:** Список Python зависимостей для Backend.

**Основные зависимости:**
- `flask>=2.0.0` — веб-фреймворк
- `gunicorn>=20.1.0` — WSGI сервер для production
- `python-dateutil>=2.8.0` — парсинг дат
- `cryptography>=3.4.0` — криптография (для x25519)
- `pytest>=7.0.0` — тестирование
- `psutil>=5.9.0` — мониторинг системных ресурсов

**Установка:**
```bash
pip install -r requirements.txt
```

---

#### `env.example`
**Назначение:** Пример файла с переменными окружения.

**Содержит:**
- `XRAY_REPORT_UI_PORT` — порт Backend (8787)
- `NEXTJS_URL` — URL Frontend
- `XRAY_CONFIG_PATH` — путь к конфигу Xray
- `XRAY_DATA_DIR` — директория данных
- `XRAY_USAGE_DIR` — директория usage логов
- `XRAY_ACCESS_LOG` — путь к access log

**Использование:** Скопировать в `.env` и настроить значения.

---

#### `README.md`
**Назначение:** Главный README проекта с описанием установки, архитектуры и использования.

---

#### `.keep-case-studies`
**Назначение:** Служебный файл для Git (чтобы сохранить пустую папку в репозитории).

---

## 📁 Директории, создаваемые при работе (не в Git)

### `data/` (создается автоматически)
**Назначение:** Директория для данных приложения.

**Содержит:**
- `settings.json` — настройки приложения
- `events.log` — лог событий
- `backups/` — бэкапы конфигурации Xray
- `live_buffer.dump` — дамп live buffer для восстановления

**Важно:** Эта папка не коммитится в Git (в `.gitignore`).

---

### `venv/` (создается при установке)
**Назначение:** Python virtual environment.

**Содержит:**
- Установленные Python пакеты из `requirements.txt`
- Изолированная среда для проекта

**Создание:**
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

**Важно:** Эта папка не коммитится в Git (в `.gitignore`).

---

### `frontend/.next/` (создается при сборке)
**Назначение:** Build output Next.js.

**Содержит:**
- Скомпилированные страницы и компоненты
- Оптимизированные статические файлы

**Создание:**
```bash
cd frontend
npm run build
```

**Важно:** Эта папка не коммитится в Git (в `.gitignore`).

---

### `frontend/node_modules/` (создается при установке)
**Назначение:** Node.js зависимости.

**Содержит:**
- Установленные npm пакеты из `frontend/package.json`

**Создание:**
```bash
cd frontend
npm install
```

**Важно:** Эта папка не коммитится в Git (в `.gitignore`).

---

## 🔄 Поток данных в системе

```
Пользователь (браузер)
    ↓
Frontend (Next.js, порт 3000)
    ↓ /api/*
Backend (Flask API, порт 8787)
    ↓
Модули (xray.py, users.py, events.py, etc.)
    ↓
Файловая система:
    - /usr/local/etc/xray/config.json (конфиг Xray)
    - /var/log/xray/access.log (access log)
    - /var/log/xray/usage/*.csv (статистика)
    - /opt/xray-report-ui/data/* (данные приложения)
    ↓
Xray-core (VPN сервер)
```

---

## 📝 Итоговая структура

```
/opt/xray-report-ui/
├── app.py                    # Главный Flask файл
├── config.py                 # Конфигурация
├── utils.py                  # Утилиты
├── cache.py                  # Кэширование
├── xray.py                   # Работа с Xray
├── users.py                  # Логика пользователей
├── events.py                 # События
├── live.py                   # Live мониторинг
├── dashboard.py              # Данные дашборда
├── settings.py               # Настройки
├── backups.py                # Бэкапы
├── system.py                 # Системные функции
├── collector.py              # Сбор статистики
│
├── api/                      # API Blueprints
│   ├── __init__.py          # Общие утилиты API
│   ├── users.py             # API пользователей
│   ├── dashboard.py         # API дашборда
│   ├── live.py              # API live мониторинга
│   ├── events.py            # API событий
│   ├── system.py            # API системы
│   ├── settings.py          # API настроек
│   ├── backups.py           # API бэкапов
│   ├── xray.py              # API Xray
│   ├── collector.py         # API коллектора
│   ├── common.py            # Общие endpoints
│   └── tests.py             # Тестовые endpoints
│
├── frontend/                 # Next.js Frontend
│   ├── app/                 # Страницы (App Router)
│   ├── components/          # React компоненты
│   ├── lib/                 # Утилиты и хуки
│   ├── types/               # TypeScript типы
│   ├── public/              # Статические файлы
│   └── package.json         # Зависимости Node.js
│
├── tests/                    # Тесты (pytest)
│   ├── conftest.py          # Конфигурация pytest
│   └── test_*.py            # Тесты модулей
│
├── scripts/                  # Bash скрипты
│   ├── ports/               # Скрипты портов
│   └── services/            # Скрипты сервисов
│
├── systemd/                  # Systemd сервисы
│   ├── xray-report-ui.service
│   └── xray-nextjs-ui.service
│
├── docs/                     # Документация
│   └── *.md                 # Множество документов
│
├── requirements.txt          # Python зависимости
├── env.example              # Пример .env файла
├── .gitignore               # Git ignore правила
├── pytest.ini              # Конфигурация pytest
└── README.md                # Главный README
│
└── [Создаются при работе:]
    ├── data/                # Данные приложения
    ├── venv/                # Python virtual environment
    └── frontend/.next/      # Build output Next.js
```

---

## 🎯 Краткое резюме по назначению

| Файл/Папка | Назначение |
|------------|-----------|
| `app.py` | Главный Flask сервер, точка входа Backend |
| `api/` | API endpoints (маршруты REST API) |
| `frontend/` | Next.js React приложение (UI) |
| `tests/` | Тесты для Backend |
| `scripts/` | Вспомогательные bash скрипты |
| `systemd/` | Конфигурация systemd сервисов |
| `docs/` | Вся документация проекта |
| `data/` | Данные приложения (создается автоматически) |
| `venv/` | Python virtual environment (создается при установке) |

---

Это полное описание структуры проекта Xray Report UI. Каждый файл и папка имеют четкое назначение и играют важную роль в работе системы.
