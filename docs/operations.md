# Операции и инфраструктура

**Дата:** 2026-01-25  
**Объединено из:** 7 файлов операций

---

## 📋 Содержание

1. [Мониторинг и стабильность](#мониторинг-и-стабильность)
2. [Оптимизация и производительность](#оптимизация-и-производительность)
3. [Анализ производительности](#анализ-производительности)
4. [Оптимизация сборки](#оптимизация-сборки)
5. [Порты сервера](#порты-сервера)
6. [Управление сервисами](#управление-сервисами)

---

# Мониторинг и стабильность

## ✅ Что было сделано для предотвращения отключения серверов

### 1. Очистка дискового пространства
- **Освобождено ~2GB** места на диске
- Настроена автоматическая ротация логов journalctl (максимум 500MB)
- Автоматическая очистка старых бэкапов (оставляется 50 последних)

---

### 2. Ограничения ресурсов в systemd

#### Frontend (xray-nextjs-ui):
- **MemoryMax**: 512MB (жесткий лимит)
- **MemoryHigh**: 400MB (мягкий лимит)
- **CPUQuota**: 150%
- **TasksMax**: 50 процессов

#### Backend (xray-report-ui):
- **MemoryMax**: 256MB (жесткий лимит)
- **MemoryHigh**: 200MB (мягкий лимит)
- **CPUQuota**: 100%
- **TasksMax**: 20 процессов

---

### 3. Улучшенный мониторинг

Скрипт `/opt/xray-report-ui/scripts/services/check.sh` теперь проверяет:
- ✅ Статус сервисов
- ✅ Доступность портов
- ✅ Использование диска (автоочистка при >85%)
- ✅ Использование памяти (алерт при >90%)
- ✅ Использование памяти каждым сервисом
- ✅ Автоматическая очистка старых бэкапов
- ✅ Автоматическая очистка логов

---

### 4. Health Checks

Скрипт `/opt/xray-report-ui/scripts/services/health-check.sh`:
- Проверяет доступность API endpoints
- Использует таймауты (5 секунд)
- Повторяет попытки (3 раза)

---

## 📊 Текущее состояние

### Диск:
- **Использование**: ~71% (было 79%)
- **Свободно**: ~5.3GB
- **Лимит алерта**: 85%

### Память:
- **Использование**: ~65%
- **Свободно**: ~1.3GB
- **Лимит алерта**: 90%

---

# Оптимизация и производительность

## Диагностика проблемы

### Типичные характеристики сервера:
- **CPU**: 3 ядра
- **RAM**: 3.7GB (доступно ~2.1GB)
- **Статус**: ⚠️ **Слабовато для сборки Next.js**

### Проблемы:
1. **Сервер слабый** - 3 ядра и 3.7GB RAM недостаточно для комфортной сборки Next.js
2. **Нет оптимизаций** - сборка выполнялась без кеширования и оптимизаций
3. **TypeScript компиляция** - каждый раз компилирует все файлы заново
4. **Большие зависимости** - 730MB node_modules

---

## ✅ Реализованные оптимизации

### 1. Оптимизация Next.js конфигурации
- ✅ Добавлено кеширование webpack (`moduleIds: 'deterministic'`)
- ✅ Оптимизация разделения чанков (code splitting)
- ✅ Параллельная сборка через `webpackBuildWorker`
- ✅ Оптимизация CSS через SWC

### 2. Оптимизация TypeScript
- ✅ Инкрементальная компиляция с кешем (`tsBuildInfoFile`)
- ✅ `skipLibCheck: true` для пропуска проверки типов в node_modules
- ✅ Возможность пропускать проверку типов при сборке

### 3. Новые инструменты
- ✅ Скрипт `frontend/build-fast.sh` для быстрой сборки
- ✅ Оптимизированные npm скрипты
- ✅ Конфигурация `.npmrc` для ускорения установки

---

## 🎯 Рекомендации по использованию

### Для разработки (90% времени):
```bash
cd /opt/xray-report-ui/frontend
npm run dev
```
**Не используйте `build` во время разработки!** Dev режим намного быстрее.

### Для проверки production сборки:
```bash
cd /opt/xray-report-ui/frontend
./build-fast.sh
```

---

# Анализ производительности

## 📊 Обзор проблем

После глубокого анализа кода выявлены критические узкие места, которые замедляют работу приложения:

### Критические проблемы (высокий приоритет)

1. **Отсутствие кэширования на бэкенде** - каждый запрос читает все CSV файлы заново
2. **Избыточные запросы с фронтенда** - множественные компоненты делают запросы каждые 5-30 секунд
3. **Синхронная обработка CSV** - блокирует обработку других запросов
4. **Дублирование запросов** - несколько компонентов запрашивают одни и те же данные

---

## 🔴 Узкие места по времени выполнения

### 1. Бэкенд: Чтение CSV файлов без кэширования

**Проблема:**
- Функция `load_dashboard_data()` читает все CSV файлы при каждом запросе
- Для 7-31 дня это означает чтение 7-31 файлов `usage_*.csv`, `conns_*.csv`, `report_*.csv`, `domains_*.csv`
- Каждый файл может содержать тысячи строк
- Время выполнения: **500ms - 3000ms** на запрос

**Влияние:**
- Запрос `/api/dashboard` выполняется 500-3000ms
- Блокирует обработку других запросов
- При нескольких одновременных запросах сервер перегружается

---

### 2. Фронтенд: Избыточные запросы с короткими интервалами

**Проблема:**
- Компоненты делают запросы каждые 5-30 секунд
- Нет централизованного кэширования (SWR не используется)
- Дублирование запросов между компонентами

**Статистика запросов:**
```
LiveNow:          каждые 5 секунд  (getLiveNow, getLiveSeries)
LiveCharts:       каждые 5 секунд  (getLiveSeries)
EventsTable:      каждые 10 секунд (getEvents)
EventsStats:      каждые 30 секунд (getEventsStats)
StatusBadges:     каждые 10 секунд (getSystemStatus)
SystemResources:  каждые 30 секунд (getSystemResources)
UserStatsCards:   каждые 30 секунд (getUserStats)
ApiStatusChecker: каждые 30 секунд (ping + getSystemResources)
```

**Влияние:**
- При открытой странице: **~20-30 запросов в минуту**
- Каждый запрос вызывает обработку CSV на бэкенде
- Перегрузка сервера при нескольких открытых вкладках

---

### 3. Бэкенд: Синхронная обработка больших CSV файлов

**Проблема:**
- Чтение CSV происходит синхронно
- Блокирует event loop Flask
- Нет параллельной обработки

**Влияние:**
- Большие CSV файлы (>10MB) читаются полностью в память
- Блокирует обработку других запросов
- Высокое использование памяти

---

### 4. Фронтенд: Отсутствие кэширования запросов

**Проблема:**
- SWR установлен в зависимостях, но не используется
- Каждый компонент делает независимые запросы
- Нет дедупликации запросов

---

## ✅ Решения

### 1. Кэширование на бэкенде

**Реализовано:**
- In-memory кэш с TTL для разных типов данных
- Автоматическая очистка старых записей
- Кэширование результатов парсинга CSV

**Результат:**
- Сокращение времени ответа с 500-3000ms до 50-200ms (при попадании в кэш)

---

### 2. Оптимизация запросов фронтенда

**Рекомендации:**
- Использовать SWR для кэширования запросов
- Увеличить интервалы обновления (5s → 10-15s)
- Объединить запросы где возможно

---

### 3. Асинхронная обработка CSV

**Рекомендации:**
- Использовать asyncio для параллельного чтения файлов
- Обрабатывать файлы в фоновых потоках
- Использовать генераторы для больших файлов

---

# Оптимизация сборки

## Проблемы и решения

### 1. Медленная сборка

#### Причины медленной сборки:

1. **Конфликт Turbopack/Webpack в Next.js 16**
   - Next.js 16 по умолчанию использует Turbopack
   - При наличии webpack конфигурации возникает конфликт
   - Решение: явно указывать `--webpack` при сборке

2. **Проверка типов TypeScript**
   - TypeScript проверка типов может занимать много времени
   - Решение: использовать `SKIP_TYPE_CHECK=true` для ускорения

3. **Проверка ESLint**
   - ESLint проверка всех файлов замедляет сборку
   - Решение: использовать `SKIP_LINT=true` для пропуска проверки

4. **Большое количество зависимостей**
   - Много библиотек увеличивают время сборки
   - Решение: оптимизация через code splitting

---

#### Команды для оптимизированной сборки:

```bash
# Стандартная сборка (с webpack)
cd /opt/xray-report-ui/frontend && npm run build

# Быстрая сборка (без проверок)
cd /opt/xray-report-ui/frontend && npm run build:fast

# Сборка с webpack (явно)
cd /opt/xray-report-ui/frontend && npm run build:webpack
```

---

#### Рекомендации для сервера:

1. **Используйте быструю сборку для production:**
   ```bash
   cd /opt/xray-report-ui/frontend && npm run build:fast
   ```

2. **Для CI/CD используйте стандартную сборку:**
   ```bash
   cd /opt/xray-report-ui/frontend && npm run build
   ```

---

## Результаты оптимизации

### До оптимизации:
- Время сборки: 5-10 минут
- Использование памяти: >2GB
- CPU: 100% на всех ядрах

### После оптимизации:
- Время сборки: 2-4 минуты (быстрая сборка)
- Использование памяти: ~1.5GB
- CPU: 80-90% (более равномерно)

---

# Порты сервера

## 📊 Текущее состояние портов

На сервере открыто **10 портов**, из которых:
- **7 критичных** - необходимы для работы системы и приложения
- **3 порта Cursor Server** - для работы Cursor через SSH (слушают только localhost)

---

## 🔐 Критичные порты (ОСТАВИТЬ)

### Порт 22 - SSH
- **Процесс:** `sshd`
- **Назначение:** Удаленный доступ к серверу
- **Доступ:** Публичный (0.0.0.0)
- **Статус:** ✅ **ОСТАВИТЬ** - необходим для управления сервером

### Порт 80 - HTTP
- **Процесс:** `nginx`
- **Назначение:** Веб-сервер HTTP
- **Доступ:** Публичный (0.0.0.0)
- **Статус:** ✅ **ОСТАВИТЬ** - необходим для веб-доступа

### Порт 443 - Xray VPN
- **Процесс:** `xray`
- **Назначение:** VPN сервер для клиентов (VLESS + Reality)
- **Доступ:** Публичный (0.0.0.0)
- **Статус:** ✅ **ОСТАВИТЬ** - основной VPN порт

### Порт 3000 - Next.js Frontend
- **Процесс:** `next-server`
- **Назначение:** Веб-интерфейс панели управления
- **Доступ:** Публичный (0.0.0.0)
- **Статус:** ✅ **ОСТАВИТЬ** - основной UI приложения

### Порт 8787 - Flask Backend
- **Процесс:** `gunicorn`
- **Назначение:** Backend API сервер
- **Доступ:** Только localhost (127.0.0.1)
- **Статус:** ✅ **ОСТАВИТЬ** - необходим для работы API

---

## 🔧 Управление портами

### Проверка портов:
```bash
# Список открытых портов
netstat -tulpn | grep LISTEN

# Проверка конкретного порта
netstat -tulpn | grep :3000
```

### Закрытие портов:
```bash
# Закрыть порт через firewall
ufw deny 3000

# Остановить сервис
systemctl stop xray-nextjs-ui
```

---

# Управление сервисами

## 📊 Текущий статус

### Production сервисы (через systemd):
- **Frontend (Next.js)**: `http://localhost:3000` или `http://YOUR_SERVER_IP:3000`
- **Backend (Flask)**: `http://localhost:8787` (только localhost)

---

## ✅ Что настроено:

1. **Автозапуск при загрузке системы** - сервисы включены (`enabled`)
2. **Автоматический перезапуск** - при падении сервисы перезапускаются через 10 секунд
3. **Мониторинг здоровья** - скрипт проверяет сервисы каждые 5 минут
4. **Логирование** - все логи сохраняются в `/var/log/`

---

## 🔧 Управление сервисами:

```bash
# Проверка статуса
systemctl status xray-nextjs-ui xray-report-ui

# Перезапуск
systemctl restart xray-nextjs-ui xray-report-ui

# Остановка
systemctl stop xray-nextjs-ui xray-report-ui

# Запуск
systemctl start xray-nextjs-ui xray-report-ui

# Просмотр логов
journalctl -u xray-nextjs-ui -f
journalctl -u xray-report-ui -f

# Проверка логов мониторинга
tail -f /var/log/xray-services-check.log
```

---

## 🚨 Если сервис не работает:

1. Проверьте статус: `systemctl status xray-nextjs-ui`
2. Проверьте логи: `journalctl -u xray-nextjs-ui -n 50`
3. Проверьте конфигурацию: `/etc/systemd/system/xray-nextjs-ui.service`
4. Перезагрузите systemd: `systemctl daemon-reload`
5. Перезапустите сервис: `systemctl restart xray-nextjs-ui`

---

## 📊 Мониторинг ресурсов

### Проверка использования памяти:
```bash
systemctl status xray-nextjs-ui | grep Memory
systemctl status xray-report-ui | grep Memory
```

### Проверка CPU:
```bash
top -p $(pgrep -f "next-server\|gunicorn")
```

---

## Автоматический мониторинг

Скрипт `/opt/xray-report-ui/scripts/services/check.sh`:
- Запускается каждые 5 минут через cron
- Проверяет статус всех сервисов
- Автоматически перезапускает упавшие сервисы
- Логирует все события в `/var/log/xray-services-check.log`

---

## Рекомендации

1. **Мониторинг:** Настроить алерты при падении сервисов
2. **Логи:** Регулярно проверять логи на ошибки
3. **Резервирование:** Настроить автоматические бэкапы
4. **Обновления:** Регулярно обновлять зависимости
