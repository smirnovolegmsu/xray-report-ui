# 🔧 Решение проблем

## Общие проблемы

### Сервисы не запускаются

**Симптомы:**
- `systemctl status` показывает `failed` или `inactive`
- Порт не слушается

**Решение:**
```bash
# Проверить статус
systemctl status xray-nextjs-ui xray-report-ui

# Проверить логи
journalctl -u xray-nextjs-ui -n 50
journalctl -u xray-report-ui -n 50

# Перезапустить
systemctl restart xray-nextjs-ui xray-report-ui
```

### Frontend не подключается к Backend

**Симптомы:**
- Ошибки в консоли браузера
- "Connection refused" или "Network error"

**Решение:**
1. Проверить, что Backend запущен: `systemctl status xray-report-ui`
2. Проверить порт: `ss -tulpn | grep 8787`
3. Проверить доступность: `curl http://localhost:8787/api/health`
4. Проверить логи Backend: `journalctl -u xray-report-ui -f`

### Порты заняты

**Симптомы:**
- Ошибка "Address already in use"
- Сервис не может запуститься

**Решение:**
```bash
# Найти процесс, использующий порт
lsof -i :3000
lsof -i :8787

# Убить процесс (если нужно)
kill -9 PID

# Или использовать скрипт анализа портов
./scripts/ports/analyze.sh
```

---

## Проблемы с Frontend

### Сборка не работает

**Симптомы:**
- Ошибки при `npm run build`
- Долгая сборка

**Решение:**
```bash
cd frontend

# Очистить кеш
rm -rf .next node_modules/.cache

# Переустановить зависимости
rm -rf node_modules
npm install

# Попробовать быструю сборку
./build-fast.sh
```

См. также [документацию по оптимизации](./optimization.md).

### TypeScript ошибки

**Симптомы:**
- Ошибки типов при сборке
- `npm run type-check` показывает ошибки

**Решение:**
```bash
cd frontend

# Проверить типы отдельно
npm run type-check

# Исправить ошибки или временно пропустить проверку
SKIP_TYPE_CHECK=true npm run build
```

### Hot reload не работает

**Симптомы:**
- Изменения не отображаются в браузере
- Нужно перезапускать сервер

**Решение:**
```bash
# Перезапустить dev сервер
cd frontend
npm run dev

# Очистить кеш браузера
# Или использовать инкогнито режим
```

---

## Проблемы с Backend

### Python зависимости не установлены

**Симптомы:**
- `ModuleNotFoundError`
- Импорты не работают

**Решение:**
```bash
# Активировать виртуальное окружение
source venv/bin/activate

# Установить зависимости
pip install flask python-dateutil

# Или создать requirements.txt и установить из него
pip install -r requirements.txt
```

### Xray API недоступен

**Симптомы:**
- Ошибки при получении статистики
- "Connection refused" к порту 10085

**Решение:**
1. Проверить, что Xray запущен: `systemctl status xray`
2. Проверить порт: `ss -tulpn | grep 10085`
3. Проверить конфиг Xray: `/usr/local/etc/xray/config.json`
4. Перезапустить Xray: `systemctl restart xray`

### Ошибки при чтении конфига

**Симптомы:**
- Ошибки при добавлении/удалении пользователей
- "Permission denied" или "File not found"

**Решение:**
```bash
# Проверить права доступа
ls -la /usr/local/etc/xray/config.json

# Проверить, что файл существует
cat /usr/local/etc/xray/config.json

# Если нужно, исправить права
sudo chmod 644 /usr/local/etc/xray/config.json
```

---

## Проблемы с производительностью

### Медленная сборка

**Симптомы:**
- Сборка занимает 5+ минут
- Сервер перегружен

**Решение:**
1. Использовать dev режим для разработки: `npm run dev`
2. Использовать быструю сборку: `./build-fast.sh`
3. Увеличить ресурсы сервера (RAM, CPU)
4. Использовать CI/CD для сборки

См. [документацию по оптимизации](./optimization.md).

### Высокое использование памяти

**Симптомы:**
- Сервер медленно работает
- Процессы используют много RAM

**Решение:**
```bash
# Проверить использование памяти
ps aux | grep -E "(next-server|python.*app.py)"

# Перезапустить сервисы
systemctl restart xray-nextjs-ui xray-report-ui

# Проверить логи на утечки памяти
journalctl -u xray-nextjs-ui --since "1 hour ago" | grep -i memory
```

---

## Проблемы с портами

### Порты не закрываются

**Симптомы:**
- Скрипты не могут закрыть порты
- Порты остаются открытыми после остановки сервисов

**Решение:**
```bash
# Найти процесс
lsof -i :PORT

# Убить процесс
kill -9 PID

# Или использовать скрипт очистки
./scripts/ports/cleanup.sh
```

### Непонятные порты открыты

**Симптомы:**
- Много открытых портов
- Непонятно, что их использует

**Решение:**
```bash
# Анализ всех портов
./scripts/ports/analyze.sh

# Оптимизация портов
./scripts/ports/optimize.sh
```

См. [документацию по портам](./ports.md).

---

## Проблемы с данными

### Потеря настроек

**Симптомы:**
- Настройки сбрасываются
- `settings.json` не сохраняется

**Решение:**
```bash
# Проверить права доступа к папке data
ls -la data/

# Проверить, что папка существует
mkdir -p data

# Исправить права
chmod 755 data
```

### Логи не пишутся

**Симптомы:**
- События не сохраняются
- `events.log` пустой

**Решение:**
```bash
# Проверить права доступа
ls -la data/events.log

# Проверить, что файл существует
touch data/events.log
chmod 644 data/events.log
```

---

## Полезные команды для диагностики

```bash
# Статус всех сервисов
systemctl status xray-nextjs-ui xray-report-ui xray

# Логи всех сервисов
journalctl -u xray-nextjs-ui -u xray-report-ui -u xray -f

# Проверка портов
ss -tulpn | grep LISTEN

# Использование ресурсов
htop
# или
ps aux | head -20

# Проверка дискового пространства
df -h

# Проверка сетевых соединений
netstat -tulpn
```

---

## Получение помощи

Если проблема не решена:

1. Проверьте логи: `journalctl -u SERVICE_NAME -n 100`
2. Проверьте документацию в папке `docs/`
3. Создайте issue в репозитории с описанием проблемы
4. Приложите логи и вывод команд диагностики

---

## Чеклист при проблемах

- [ ] Проверить статус сервисов
- [ ] Проверить логи
- [ ] Проверить порты
- [ ] Проверить права доступа к файлам
- [ ] Проверить зависимости (Python и Node.js)
- [ ] Проверить конфигурацию
- [ ] Перезапустить сервисы
- [ ] Очистить кеш (если проблема с Frontend)
