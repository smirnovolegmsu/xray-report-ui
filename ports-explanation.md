# Подробное объяснение всех портов на сервере

## 🔐 Системные порты (критичные)

### Порт 22 (TCP) - SSH
- **Процесс:** `sshd`
- **Назначение:** Удаленный доступ к серверу по протоколу SSH
- **Доступ:** Публичный (доступен извне)
- **Зачем нужен:** Позволяет подключаться к серверу для управления через терминал
- **Можно ли закрыть:** ❌ НЕТ - без него не будет доступа к серверу
- **Firewall:** Разрешен в UFW

### Порт 53 (TCP/UDP) - DNS
- **Процесс:** `systemd-resolved`
- **Назначение:** Разрешение доменных имен (DNS кэш)
- **Доступ:** Только localhost (127.0.0.53)
- **Зачем нужен:** Преобразует доменные имена (например, google.com) в IP-адреса
- **Можно ли закрыть:** ❌ НЕТ - критично для работы системы и приложений
- **Firewall:** Не требуется (только localhost)

### Порт 123 (UDP) - NTP
- **Процесс:** `ntpd`
- **Назначение:** Синхронизация времени (Network Time Protocol)
- **Доступ:** Публичный (для синхронизации с NTP серверами)
- **Зачем нужен:** Поддерживает точное время на сервере, критично для логов, сертификатов и работы приложений
- **Можно ли закрыть:** ❌ НЕТ - без него время на сервере будет расходиться
- **Firewall:** Не требуется (исходящие соединения)

## 🌐 Веб-сервисы (критичные)

### Порт 80 (TCP) - HTTP
- **Процесс:** `nginx`
- **Назначение:** Веб-сервер HTTP
- **Доступ:** Публичный (доступен извне)
- **Зачем нужен:** Обслуживает HTTP запросы, может использоваться для редиректа на HTTPS или статических файлов
- **Можно ли закрыть:** ❌ НЕТ - необходим для веб-доступа
- **Firewall:** Разрешен в UFW

### Порт 3000 (TCP) - Next.js Frontend
- **Процесс:** `next-server`
- **Назначение:** Веб-интерфейс панели управления Xray Report UI
- **Доступ:** Публичный (доступен извне)
- **Зачем нужен:** Основной пользовательский интерфейс для управления VPN сервером, пользователями, статистикой
- **Можно ли закрыть:** ❌ НЕТ - это основной UI приложения
- **Firewall:** Разрешен в UFW
- **URL:** `http://YOUR_SERVER_IP:3000`

### Порт 8787 (TCP) - Flask Backend API
- **Процесс:** `python` (Flask)
- **Назначение:** REST API для управления Xray и пользователями
- **Доступ:** Только localhost (127.0.0.1) - безопасен
- **Зачем нужен:** Обрабатывает все API запросы от фронтенда: управление пользователями, статистика, настройки
- **Можно ли закрыть:** ❌ НЕТ - без него фронтенд не будет работать
- **Firewall:** Не требуется (только localhost)
- **Эндпоинты:** `/api/users`, `/api/settings`, `/api/dashboard`, etc.

## 🔐 VPN сервисы (критичные)

### Порт 443 (TCP) - Xray VPN
- **Процесс:** `xray`
- **Назначение:** Основной VPN сервер для клиентов (VLESS + Reality протокол)
- **Доступ:** Публичный (доступен извне)
- **Зачем нужен:** Это главный порт, через который подключаются VPN клиенты. Использует протоколы VLESS и Reality для обхода блокировок
- **Можно ли закрыть:** ❌ НЕТ - это основной VPN порт, без него VPN не будет работать
- **Firewall:** Разрешен в UFW
- **Протокол:** VLESS + Reality (маскируется под обычный HTTPS трафик)

### Порт 1194 (UDP) - OpenVPN
- **Процесс:** `openvpn`
- **Назначение:** Дополнительный VPN сервер (OpenVPN протокол)
- **Доступ:** Публичный (доступен извне)
- **Зачем нужен:** Альтернативный VPN протокол, если используется OpenVPN вместо Xray
- **Можно ли закрыть:** ✅ ДА - если вы используете только Xray и не используете OpenVPN
- **Firewall:** Не настроен в UFW (можно закрыть)
- **Команда для отключения:** `sudo systemctl stop openvpn && sudo systemctl disable openvpn`

### Порт 10085 (TCP) - Xray API
- **Процесс:** `xray`
- **Назначение:** Внутренний API для управления Xray (Stats API)
- **Доступ:** Только localhost (127.0.0.1) - безопасен
- **Зачем нужен:** Используется Flask Backend для получения статистики Xray: количество подключений, трафик, онлайн пользователи
- **Можно ли закрыть:** ❌ НЕТ - без него панель управления не сможет получать статистику
- **Firewall:** Не требуется (только localhost)

### Порты 39989, 49909, 52120, 52656, 59801, 60681 (UDP) - Xray UDP
- **Процесс:** `xray`
- **Назначение:** UDP порты для VPN трафика
- **Доступ:** Публичный (для UDP трафика)
- **Зачем нужны:** Xray использует динамические UDP порты для передачи VPN трафика. Эти порты открываются автоматически при работе VPN и используются для:
  - UDP трафика VPN клиентов
  - Оптимизации производительности
  - Поддержки различных VPN протоколов
- **Можно ли закрыть:** ❌ НЕТ - необходимы для работы Xray VPN
- **Firewall:** UDP порты обычно не блокируются
- **Примечание:** Количество и номера портов могут меняться в зависимости от нагрузки и конфигурации Xray

## 💻 Cursor Server (опциональные)

### Порты 37011, 40783, 46705 (TCP) - Cursor Server
- **Процесс:** `node` (Cursor Server)
- **Назначение:** Сервер для работы Cursor IDE через SSH
- **Доступ:** Только localhost (127.0.0.1) - безопасен
- **Зачем нужны:** Позволяют использовать Cursor IDE для разработки через SSH туннель. Порты используются для:
  - **37011:** Cursor Server Main - основной сервер
  - **40783:** Cursor Extension Host - хост расширений
  - **46705:** Cursor Multiplex Server - мультиплексный сервер
- **Можно ли закрыть:** ✅ ДА - если вы не используете Cursor IDE для разработки через SSH
- **Firewall:** Не требуется (только localhost)
- **Команда для закрытия:** `./cleanup-ports.sh --kill-cursor`
- **Примечание:** Эти порты безопасны, так как слушают только на localhost и недоступны извне

## 📊 Сводная таблица

| Порт | Протокол | Процесс | Назначение | Доступ | Критичность |
|------|----------|---------|------------|--------|-------------|
| 22 | TCP | sshd | SSH доступ | Публичный | 🔴 Критичный |
| 53 | TCP/UDP | systemd-resolved | DNS | localhost | 🔴 Критичный |
| 80 | TCP | nginx | HTTP | Публичный | 🔴 Критичный |
| 123 | UDP | ntpd | NTP (время) | Публичный | 🔴 Критичный |
| 443 | TCP | xray | Xray VPN | Публичный | 🔴 Критичный |
| 1194 | UDP | openvpn | OpenVPN | Публичный | 🟡 Опциональный |
| 3000 | TCP | next-server | Next.js UI | Публичный | 🔴 Критичный |
| 8787 | TCP | python | Flask API | localhost | 🔴 Критичный |
| 10085 | TCP | xray | Xray API | localhost | 🔴 Критичный |
| 37011 | TCP | node | Cursor Server | localhost | 🟡 Опциональный |
| 40783 | TCP | node | Cursor Extension | localhost | 🟡 Опциональный |
| 46705 | TCP | node | Cursor Multiplex | localhost | 🟡 Опциональный |
| 39989+ | UDP | xray | Xray UDP трафик | Публичный | 🔴 Критичный |

## 🔍 Как проверить порт

Для проверки конкретного порта используйте:
```bash
# Проверить, слушает ли порт
ss -tulpn | grep :PORT

# Детальная информация о порте
lsof -i :PORT

# Проверить процесс
ps aux | grep PID
```

## ⚠️ Важные замечания

1. **Порты на localhost (127.0.0.1)** безопасны и недоступны извне
2. **UDP порты Xray** динамические - их количество может меняться
3. **ESTABLISHED соединения** - это временные порты клиентов, их не нужно закрывать
4. **Опциональные порты** можно закрыть, если не используются
