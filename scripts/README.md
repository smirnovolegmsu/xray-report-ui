# 📜 Скрипты проекта

Эта папка содержит вспомогательные скрипты для управления проектом.

---

## 📁 Структура

```
scripts/
├── ports/              # Скрипты для работы с портами
│   ├── analyze.sh     # Анализ всех открытых портов
│   ├── cleanup.sh     # Очистка неиспользуемых портов
│   └── optimize.sh    # Оптимизация портов
└── services/          # Скрипты для работы с сервисами
    └── check.sh       # Проверка и перезапуск сервисов
```

---

## 🔌 Скрипты для работы с портами

### `ports/analyze.sh`

Анализирует все открытые порты на сервере и показывает детальную информацию о каждом.

**Использование:**
```bash
./scripts/ports/analyze.sh
```

**Что делает:**
- Показывает все открытые порты
- Определяет процесс, использующий порт
- Классифицирует порты (критичные, опциональные)
- Дает рекомендации по каждому порту

---

### `ports/cleanup.sh`

Очищает неиспользуемые порты.

**Использование:**
```bash
# Просмотр, что будет закрыто (безопасно, без изменений)
./scripts/ports/cleanup.sh --dry-run

# Закрыть неиспользуемые порты
./scripts/ports/cleanup.sh

# Закрыть порты Cursor Server (если не используете)
./scripts/ports/cleanup.sh --kill-cursor
```

**Что делает:**
- Находит неиспользуемые порты
- Закрывает временные порты отладки
- Опционально закрывает порты Cursor Server

---

### `ports/optimize.sh`

Интерактивная оптимизация портов с рекомендациями.

**Использование:**
```bash
./scripts/ports/optimize.sh
```

**Что делает:**
- Показывает все варианты оптимизации
- Дает рекомендации по каждому порту
- Помогает принять решение о закрытии портов

---

## 🔧 Скрипты для работы с сервисами

### `services/check.sh`

Проверяет статус сервисов и автоматически перезапускает их при проблемах.

**Использование:**
```bash
# Ручной запуск проверки
./scripts/services/check.sh

# Настройка автоматической проверки (cron)
# Добавить в crontab для проверки каждые 5 минут:
*/5 * * * * /opt/xray-report-ui/scripts/services/check.sh
```

**Что делает:**
- Проверяет статус сервисов `xray-nextjs-ui` и `xray-report-ui`
- Проверяет доступность портов (3000 и 8787)
- Автоматически перезапускает сервисы при проблемах
- Логирует все действия в `/var/log/xray-services-check.log`

---

## 🚀 Быстрый старт

### Анализ портов
```bash
./scripts/ports/analyze.sh
```

### Проверка сервисов
```bash
./scripts/services/check.sh
```

### Оптимизация портов
```bash
./scripts/ports/optimize.sh
```

---

## 📝 Примечания

- Все скрипты требуют выполнения из корня проекта: `/opt/xray-report-ui/`
- Некоторые скрипты могут требовать прав root для определенных операций
- Скрипты логируют свои действия для отладки
- Используйте `--dry-run` для безопасного просмотра изменений

---

## 🔗 Связанная документация

- [Документация по портам](../docs/ports.md)
- [Документация по сервисам](../docs/services.md)
- [Руководство разработчика](../docs/development.md)
