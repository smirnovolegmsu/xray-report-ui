# 🧹 План очистки проекта

## 🗑️ Что УДАЛИТЬ (мусор)

### 1. `history/` (940 KB) - УДАЛИТЬ
**Что:** 17 старых версий app.py из разработки  
**Зачем было:** История разработки, промежуточные версии  
**Почему удалить:** Это всё есть в Git, не нужно дублировать  
```bash
rm -rf /opt/xray-report-ui/history/
```

### 2. `____back_up - 20251231_1136/` (100 KB) - УДАЛИТЬ
**Что:** Старый бэкап кода  
**Зачем было:** Страховка перед изменениями  
**Почему удалить:** Устарел, есть Git  
```bash
rm -rf "/opt/xray-report-ui/____back_up - 20251231_1136/"
```

### 3. `____back_up - 20251231_1506/` (104 KB) - УДАЛИТЬ
**Что:** Еще один старый бэкап кода  
**Зачем было:** Страховка перед изменениями  
**Почему удалить:** Устарел, есть Git  
```bash
rm -rf "/opt/xray-report-ui/____back_up - 20251231_1506/"
```

### 4. `state/` (64 KB) - УДАЛИТЬ
**Что:** Дубликаты файлов (events.log, user_aliases.json)  
**Зачем было:** Неясно, возможно ошибка  
**Почему удалить:** Всё уже есть в `data/`  
```bash
rm -rf /opt/xray-report-ui/state/
```

### 5. `__pycache__/` - ИГНОРИРОВАТЬ (автогенерируется)
**Что:** Кеш Python  
**Зачем:** Ускорение загрузки Python модулей  
**Что делать:** Добавить в .gitignore, не трогать вручную

### 6. `index.html.backup` (324 KB) - ОСТАВИТЬ но исключить из Git
**Что:** Бэкап старого HTML  
**Зачем:** Страховка на случай проблем  
**Что делать:** Оставить локально, но добавить в .gitignore

### 7. `user_aliases.json` (корень) - УДАЛИТЬ
**Что:** Дубликат, уже есть в data/  
**Почему удалить:** Дубль  
```bash
rm -f /opt/xray-report-ui/user_aliases.json
```

### 8. `check_release.py` - УДАЛИТЬ?
**Что:** Скрипт проверки релизов  
**Зачем:** Непонятно  
**Решение:** Нужно проверить используется ли

---

## ✅ Что ОСТАВИТЬ (рабочие файлы)

### Структура проекта:

```
/opt/xray-report-ui/
├── app.py                      # Главный Flask приложение
├── config.py                   # Конфигурация
│
├── templates/                  # HTML шаблоны
│   └── index.html             # Новый модульный HTML
│
├── static/                     # Статические файлы
│   ├── css/
│   │   └── styles.css         # Все стили
│   └── js/
│       ├── i18n.js            # Переводы
│       ├── utils.js           # Утилиты
│       ├── theme.js           # Темы
│       ├── state.js           # Состояние
│       ├── management.js      # История
│       ├── online.js          # Онлайн
│       ├── users.js           # Пользователи
│       ├── events.js          # События
│       ├── settings.js        # Настройки
│       ├── collector.js       # Сборщик
│       ├── system.js          # Система
│       └── init.js            # Инициализация
│
├── core/                       # Бизнес-логика (Python)
│   ├── __init__.py
│   ├── events.py              # Логирование событий
│   ├── backup.py              # Бэкапы
│   └── settings.py            # Управление настройками
│
├── utils/                      # Утилиты (Python)
│   ├── __init__.py
│   ├── file_ops.py            # Файловые операции
│   ├── date_utils.py          # Работа с датами
│   └── system.py              # systemctl операции
│
├── api/                        # API routes (пока пустая)
│   └── __init__.py
│
├── data/                       # Рабочие данные (НЕ в Git!)
│   ├── settings.json          # Настройки приложения
│   ├── events.log             # Лог событий
│   ├── usage_live.json        # Live статистика
│   └── backups/               # Бэкапы конфигов Xray
│       └── *.json
│
├── venv/                       # Python виртуальное окружение
│
├── *.md                        # Документация
└── .git/                       # Git репозиторий
```

---

## 📋 .gitignore (что НЕ добавлять в Git)

```gitignore
# Python
__pycache__/
*.pyc
*.pyo
venv/

# Данные приложения
data/
*.backup

# IDE
.cursor/
.vscode/
.idea/

# Временные файлы
*.tmp
*.log
.DS_Store

# Бэкапы
*_back_up*/
history/
```

---

## 🎯 Итоговая простая структура

**3 главные папки:**

1. **Frontend** (static/ + templates/)
   - HTML, CSS, JavaScript
   - Всё что показывается в браузере

2. **Backend** (app.py + core/ + utils/ + api/)
   - Python код
   - Бизнес-логика и API

3. **Данные** (data/)
   - Настройки, логи, статистика
   - НЕ в Git!

**Итого:**
- ❌ Удалить: ~1.2 MB мусора
- ✅ Оставить: ~300 KB рабочего кода
- 📁 Структура: Простая и понятная

---

## 🚀 Команды для очистки

```bash
cd /opt/xray-report-ui

# 1. Удалить мусор
rm -rf history/
rm -rf "____back_up - 20251231_1136/"
rm -rf "____back_up - 20251231_1506/"
rm -rf state/
rm -f user_aliases.json

# 2. Создать .gitignore
cat > .gitignore << 'EOF'
# Python
__pycache__/
*.pyc
*.pyo
venv/

# Данные приложения
data/
*.backup

# IDE
.cursor/
.vscode/
.idea/

# Временные
*.tmp
*.log
.DS_Store

# История/бэкапы
*_back_up*/
history/
state/
EOF

# 3. Удалить из Git (если уже добавлено)
git rm -r --cached __pycache__/ 2>/dev/null
git rm -r --cached history/ 2>/dev/null
git rm -r --cached state/ 2>/dev/null
git rm --cached "*.backup" 2>/dev/null

# 4. Проверить результат
du -sh */ | sort -h
```
