# 📁 Структура проекта Xray Report UI

## 🎯 Простая и логичная организация

### Основная идея:
```
Frontend (что видит пользователь) = static/ + templates/
Backend (логика на сервере)        = app.py + core/ + utils/ + api/
Данные (не в Git)                  = data/ + venv/
```

---

## 📂 Детальное описание

### 1. **Frontend** - Всё что в браузере

```
templates/
  └── index.html                    # HTML структура страницы
                                    # - Заголовки, меню, карточки
                                    # - Минимум кода, только разметка

static/
  ├── css/
  │   └── styles.css                # Все стили приложения
  │                                 # - Цвета, размеры, анимации
  │                                 # - Темная/светлая тема
  │
  └── js/                           # JavaScript модули
      ├── i18n.js                   # Переводы (RU/EN)
      ├── utils.js                  # Вспомогательные функции
      ├── theme.js                  # Переключение тем
      ├── state.js                  # Глобальное состояние
      ├── management.js             # Страница "История"
      ├── online.js                 # Страница "Онлайн"
      ├── users.js                  # Страница "Пользователи"
      ├── events.js                 # Страница "События"
      ├── settings.js               # Страница "Настройки"
      ├── collector.js              # Статус сборщика
      ├── system.js                 # Системная информация
      └── init.js                   # Инициализация приложения
```

**Как работает:**
1. Браузер загружает `index.html`
2. `index.html` подключает `styles.css` и все `*.js`
3. JavaScript модули делают страницу интерактивной
4. Все данные получаются через API запросы к backend

---

### 2. **Backend** - Логика на сервере

```
app.py                              # 🔥 Главный файл Flask
                                    # - Создает веб-сервер
                                    # - Регистрирует все API routes
                                    # - Запускает приложение

config.py                           # ⚙️ Конфигурация
                                    # - Пути к файлам
                                    # - Настройки по умолчанию
                                    # - Константы

core/                               # 💼 Бизнес-логика
  ├── events.py                     # - Логирование событий
  ├── backup.py                     # - Бэкапы конфигов
  ├── settings.py                   # - Управление настройками
  ├── xray.py          (TODO)       # - Работа с Xray конфигом
  ├── usage.py         (TODO)       # - Парсинг CSV статистики
  └── live_monitor.py  (TODO)       # - Real-time мониторинг

utils/                              # 🛠️ Утилиты
  ├── file_ops.py                   # - Атомарные записи файлов
  ├── date_utils.py                 # - Работа с датами
  └── system.py                     # - systemctl операции

api/                    (TODO)      # 🌐 API Endpoints
  ├── users.py                      # - /api/users/*
  ├── dashboard.py                  # - /api/dashboard, /api/usage/*
  ├── live.py                       # - /api/live/*
  ├── system.py                     # - /api/system/*, /api/xray/*
  └── settings.py                   # - /api/settings
```

**Как работает:**
1. `app.py` запускает Flask сервер на порту 8787
2. Frontend делает запрос типа `GET /api/users`
3. Flask находит соответствующий route в `app.py`
4. Route вызывает функцию из `core/` или `utils/`
5. Функция обрабатывает данные и возвращает JSON
6. Frontend получает JSON и отображает данные

**Пример потока:**

```
Браузер → GET /api/users
              ↓
         app.py (route @app.get("/api/users"))
              ↓
         core/xray.py (функция get_xray_clients())
              ↓
         utils/file_ops.py (читает config.json)
              ↓
         Возвращает JSON с пользователями
              ↓
         Браузер отображает таблицу
```

---

### 3. **Данные** - Не добавляется в Git

```
data/
  ├── settings.json                 # Настройки приложения
  │                                 # - server_host, reality_pbk
  │                                 # - Локаль, параметры сборщика
  │
  ├── events.log                    # Лог всех действий
  │                                 # - Добавление пользователей
  │                                 # - Перезапуски сервисов
  │
  ├── usage_live.json               # Live статистика
  │                                 # - Текущие подключения
  │                                 # - Real-time трафик
  │
  └── backups/                      # Бэкапы конфигов Xray
      ├── 20260107_162318__clients_update__config.json
      └── ...

venv/                               # Python виртуальное окружение
                                    # - Flask, библиотеки
                                    # - НЕ добавляется в Git
```

**Почему не в Git:**
- Эти файлы уникальны для каждого сервера
- Содержат конфиденциальные данные
- Меняются постоянно

---

## 🔄 Как всё работает вместе

### Сценарий 1: Пользователь открывает страницу

```
1. Браузер → GET http://server:8787/
2. app.py → Отдает templates/index.html
3. Браузер загружает:
   - static/css/styles.css
   - static/js/*.js (12 файлов)
4. init.js запускается и:
   - Делает GET /api/settings
   - Делает GET /api/users
   - Делает GET /api/dashboard
5. app.py обрабатывает запросы через core/*
6. Браузер получает данные и рисует UI
```

### Сценарий 2: Пользователь добавляет юзера

```
1. Браузер (users.js) → POST /api/users/add {email: "user1"}
2. app.py (route) → core/xray.py (set_xray_clients())
3. core/xray.py:
   - Читает config.json через utils/file_ops.py
   - Добавляет нового юзера
   - Делает бэкап через core/backup.py
   - Сохраняет config.json
   - Логирует событие через core/events.py
4. Возвращает JSON {ok: true}
5. Браузер обновляет таблицу пользователей
```

### Сценарий 3: Live мониторинг

```
1. Каждые 60 секунд:
   - Фоновый поток в app.py
   - core/live_monitor.py парсит /var/log/xray/access.log
   - Сохраняет в data/usage_live.json

2. Браузер (online.js):
   - Каждые 5 секунд делает GET /api/live/now
   - Получает текущую статистику
   - Обновляет графики
```

---

## 📦 Зависимости между модулями

```
app.py
  ├── использует config.py
  ├── использует core/events.py
  ├── использует core/backup.py
  ├── использует core/settings.py
  └── (TODO) будет использовать api/*

core/events.py
  ├── использует config.py
  ├── использует utils/date_utils.py
  └── использует utils/file_ops.py

core/backup.py
  ├── использует config.py
  ├── использует utils/date_utils.py
  └── использует utils/file_ops.py

core/settings.py
  ├── использует config.py
  └── использует utils/file_ops.py

utils/* - независимые, ничего не импортируют
```

**Правило:** Зависимости идут сверху вниз:
- `app.py` может импортировать всё
- `core/*` может импортировать `config` и `utils/*`
- `utils/*` ничего не импортируют (кроме stdlib)

---

## 🎯 Преимущества структуры

### 1. **Понятно куда что класть**
- Новый JS код? → `static/js/`
- Новая бизнес-логика? → `core/`
- Новая утилита? → `utils/`
- Новый API endpoint? → `api/`

### 2. **Легко найти**
- Ошибка в графике? → Смотрим `static/js/management.js`
- Проблема с Xray? → Смотрим `core/xray.py`
- Не работает бэкап? → Смотрим `core/backup.py`

### 3. **Модульность**
- Можно заменить `static/` на React/Vue
- Можно вынести `api/` в отдельный микросервис
- Можно переписать `core/` на другой язык

### 4. **Тестируемость**
- Каждый модуль можно тестировать отдельно
- `utils/*` - чистые функции, легко тестировать
- `core/*` - бизнес-логика, критично тестировать

---

## 📝 Резюме

**Проект разделен на 3 части:**

1. **Frontend** (static/ + templates/) - Что видит пользователь
2. **Backend** (app.py + core/ + utils/) - Логика на сервере
3. **Данные** (data/ + venv/) - Не в Git

**Как работает:**
- Браузер загружает HTML/CSS/JS
- JS делает API запросы
- Backend обрабатывает через core/ и utils/
- Данные хранятся в data/

**Просто, логично, масштабируемо!** 🎉
